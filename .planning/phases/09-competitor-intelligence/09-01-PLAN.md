---
phase: 09-competitor-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - htg_config.csv
  - docs/HOWTOGENIE.md
  - growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
user_setup: []

must_haves:
  truths:
    - Workflow runs on schedule (e.g. every 3h) and reads RSS + Reddit from config
    - Workflow writes deduplicated, recency-ordered trend list to one config-driven Sheet tab
    - Config Loader runs first; after each HTTP there is an IF (status) and a Wait (delay) to avoid 429/blocking
    - No hardcoded YOUR_GOOGLE_SHEET_ID or sheet names in the workflow JSON
  artifacts:
    - path: htg_config.csv
      provides: COMPETITOR_INTEL_TAB key for trend list tab name
      contains: "COMPETITOR_INTEL_TAB"
    - path: growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json
      provides: Refactored competitor workflow with Config Loader, config-driven sources and sheet
  key_links:
    - from: Schedule Trigger
      to: Execute Workflow (Config Loader)
      via: first node after trigger
    - from: Code that builds source list
      to: COMPETITOR_RSS_FEEDS and REDDIT_SUBREDDITS
      via: $('⚙️ Load Config').item.json
    - from: Each HTTP Request (RSS, Reddit, Google Trends)
      to: IF (2xx) and Wait (e.g. 2s)
      via: node connections
    - from: Merge/dedupe Code output
      to: Google Sheets write
      via: documentId = config.GOOGLE_SHEET_ID, sheetName = config.COMPETITOR_INTEL_TAB
---

<objective>
Refactor the Competitor Intelligence workflow to run on schedule, read RSS and Reddit from config (COMPETITOR_RSS_FEEDS, REDDIT_SUBREDDITS), apply delay/IF after each HTTP, and write a deduplicated recency-ordered trend list to a single config-driven Sheet tab. No hardcoded sheet ID or tab names.

Purpose: Satisfy COMP-01–COMP-04; single source of truth for config; rate-limit-safe fetches.
Output: Updated htg_config.csv (COMPETITOR_INTEL_TAB), docs/HOWTOGENIE.md (config key row), and growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json (Config Loader first, config-driven sources and sheet, IF+Wait after HTTP, one trend-list tab).
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-competitor-intelligence/09-CONTEXT.md
@.planning/phases/09-competitor-intelligence/09-RESEARCH.md
@.planning/phases/09-competitor-intelligence/09-VALIDATION.md

Patterns: Config Loader is invoked via Execute Workflow; downstream nodes use `$('⚙️ Load Config').item.json.<KEY>`. Topic Research and SEO Interlinking use this pattern. Use same node name "⚙️ Load Config" so expressions work. Config Loader workflow is in core/01_Config_Loader.json; caller references it by workflowId (set at import). Plain JS in Code nodes; no YOUR_* in workflow JSON.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config key and Config Loader + config-driven source list</name>
  <files>htg_config.csv, docs/HOWTOGENIE.md, growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json</files>
  <action>
1. Add COMPETITOR_INTEL_TAB to htg_config.csv (e.g. value "Competitor Intelligence"). Add a row to the Config Keys table in docs/HOWTOGENIE.md under "APIs & external" or "Competitor" so COMPETITOR_INTEL_TAB is documented with COMPETITOR_RSS_FEEDS.

2. In the competitor workflow JSON:
   - Insert an Execute Workflow node immediately after the Schedule Trigger. Configure it to run the Config Loader workflow (workflowId — use placeholder or same pattern as content/02_Topic_Research_Engine_v2.json Execute Workflow). Name the node exactly "⚙️ Load Config".
   - Replace the "🎯 Load Monitoring Targets" Code node: remove hardcoded competitors and subreddits. New logic: read config from $('⚙️ Load Config').item.json (or $input.first().json if this node receives Config Loader output). Parse COMPETITOR_RSS_FEEDS (comma-split, trim) and REDDIT_SUBREDDITS (comma-split, trim; normalize to r/name if needed). Output one item per RSS URL: { type: 'rss', url: fullUrl }. Output one item per subreddit: { type: 'reddit', subreddit: 'r/name' or name }. If both lists are empty, return a single item { no_sources: true }. Do not use base URL + /feed/ — COMPETITOR_RSS_FEEDS contains full feed URLs.

3. Fix RSS and Reddit fetch nodes to use config-driven data:
   - RSS HTTP Request: use the item's full url (e.g. ={{ $json.url }}) — not $json.url + '/feed/'.
   - Reddit HTTP Request: use item.subreddit (e.g. https://www.reddit.com/{{ $json.subreddit.replace(/^r\//, '') }}/hot.json?limit=25). Keep User-Agent: HowToGenie-Bot/1.0.

4. Remove or bypass the Ahrefs/Backlink branch for Phase 9 (delete "🔗 Fetch Our Backlink Profile", "🔗 Fetch Competitor Backlinks", "🔗 AI: Backlink Gap Analysis", "📋 Queue Backlink Outreach" and any "Backlink Opportunities" Google Sheets node, or route so they are never executed). Remove "📝 Add to Content Ideas Queue" and "📊 Log Intelligence Report" Google Sheets nodes (we will write a single trend list in Task 2). Disconnect the LLM "🧠 AI: Analyze Intelligence & Generate Ideas" and "📋 Parse Intelligence Report" from the main flow so the pipeline is: sources → HTTP → IF → Parse → merge → dedupe/sort → one Sheets write.

5. Ensure no literal YOUR_GOOGLE_SHEET_ID, "Competitor Intelligence", "Content Ideas Queue", or "Backlink Opportunities" remains in the JSON. All sheet references will be replaced in Task 2 with config expressions.
  </action>
  <verify>
    <automated>grep -qE "YOUR_GOOGLE_SHEET_ID|Content Ideas Queue|Backlink Opportunities" "growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json" && exit 1; grep -q "COMPETITOR_INTEL_TAB" htg_config.csv; node -e "JSON.parse(require('fs').readFileSync('growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json'));"</automated>
  </verify>
  <done>COMPETITOR_INTEL_TAB in htg_config.csv and doc; Execute Workflow "⚙️ Load Config" first; source list from config; RSS/Reddit use item url/subreddit; Ahrefs and old Sheets/LLM write paths removed; no YOUR_* or hardcoded tab names in file.</done>
</task>

<task type="auto">
  <name>Task 2: Delay and IF after HTTP; merge, dedupe, sort, single Sheets write</name>
  <files>growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json</files>
  <action>
1. After each HTTP Request node (RSS fetch, Reddit fetch, Google Trends fetch): insert an IF node that checks for success (e.g. response status code 2xx or presence of body). On true → connect to existing Parse node. On false → connect to a Code or Set node that returns one item with empty array or structured error (e.g. { items: [], error: true }) so the merge step receives something. Insert a Wait node (n8n Wait, "After Time Interval", e.g. 2 seconds) after each HTTP (or after the Parse branch) so that when processing multiple items (Loop Over Items or similar), the next request is delayed. This satisfies COMP-03.

2. Merge all branches (RSS parsed items, Reddit parsed items, Google Trends parsed items). Use a Merge node or a Code node that aggregates $input.all() from the parse outputs. For Google Trends: keep best-effort — if IF false (non-200 or parse error), the error path should pass through empty items so the workflow continues.

3. Add a Code node that: receives all trend items (each item has at least url, title, source, published or detected_at); deduplicates by url (keep one per url, prefer row with later published/detected_at); sorts by date descending (published || detected_at); optionally caps at 500 rows; outputs array of { title, url, source, published, detected_at } (or same shape as existing parse outputs). Return one item per row for Google Sheets: return sorted.map(r => ({ json: r })).

4. Add a single Google Sheets node: operation = "append" with "clear" of the sheet range first, OR use "update" in a way that replaces the trend list (e.g. clear sheet then append, or document that replace is done by range clear + append). documentId = {{ $('⚙️ Load Config').item.json.GOOGLE_SHEET_ID }}, sheetName = {{ $('⚙️ Load Config').item.json.COMPETITOR_INTEL_TAB }} (no literal fallback in JSON so COMP-04 grep passes). Map columns to the trend row shape (e.g. Title, URL, Source, Published, Detected At). Ensure this is the only Google Sheets write in the workflow for the trend list. No YOUR_* or literal tab names.

5. Wire the flow: Trigger → ⚙️ Load Config → Build source list (Code) → Route by type → [per type: HTTP → IF → Parse → Wait] → Merge → Dedupe/sort Code → Google Sheets. Remove or disconnect any remaining old nodes that wrote to Content Ideas Queue, Competitor Intelligence (summary), or Backlink Opportunities.
  </action>
  <verify>
    <automated>grep -qE "YOUR_GOOGLE_SHEET_ID|\"Competitor Intelligence\"|Content Ideas Queue|Backlink" "growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json" && exit 1; grep -q "Wait\|wait" "growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json"; grep -q "Load Config" "growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json"; node -e "const w=require('fs').readFileSync('growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json'); const j=JSON.parse(w); const sheets=j.nodes.filter(n=>n.type&&n.type.includes('googleSheets')); if(sheets.length!==1) throw new Error('Expected exactly one Google Sheets node'); console.log('OK');"</automated>
  </verify>
  <done>IF and Wait after each HTTP; one merge → dedupe/sort Code → one Google Sheets write with config-driven documentId and sheetName; workflow has exactly one Sheets node for the trend list; COMP-02 and COMP-03 satisfied.</done>
</task>

</tasks>

<verification>
- Run: grep -E "YOUR_GOOGLE_SHEET_ID|Competitor Intelligence|Content Ideas Queue|Backlink" "growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json" → must return no matches (COMP-04).
- Run: node -e "JSON.parse(require('fs').readFileSync('growth/HowTo-Genie v4.0 — Competitor Intelligence & Trend Monitor.json'))" → valid JSON.
- Manual (per 09-VALIDATION.md): Import workflow in n8n; run with Config Loader and test sheet; confirm at least one RSS and one Reddit source run, delay between requests, and trend list written to config-driven tab.
</verification>

<success_criteria>
- COMP-01: Schedule (e.g. 0 */3 * * *) and RSS/Reddit sources read from config.
- COMP-02: Deduplicated, recency-ordered trend list written to one config-driven Sheet tab.
- COMP-03: Config Loader first; delay and IF after each HTTP.
- COMP-04: No hardcoded YOUR_GOOGLE_SHEET_ID or sheet names in workflow JSON.
</success_criteria>

<output>
After completion, create .planning/phases/09-competitor-intelligence/09-01-SUMMARY.md
</output>
