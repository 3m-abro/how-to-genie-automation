---
phase: 07-docs-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/HOWTOGENIE.md
  - docs/ORCHESTRATOR-README.md
  - docs/howto-genie-setup-guide.md
  - CLAUDE.md
autonomous: false
requirements:
  - DOCS-01
  - DOCS-02
must_haves:
  truths:
    - One consolidated Markdown doc exists and covers workflows, UI, config keys, schedule, and archive
    - Doc is the single authoritative reference; no other human-facing setup doc competes
    - Workflow list and config key reference are present so the doc can be updated when adding workflows or config
    - CLAUDE.md points to the consolidated doc for workflow list and config (no duplicate long-form content)
  artifacts:
    - path: docs/HOWTOGENIE.md
      provides: Single authoritative reference for workflows, UI, config, schedule, archive
      contains: "## Workflows", "## Config Keys", "## Schedule", "## UI", "## Archive"
    - path: docs/ORCHESTRATOR-README.md
      provides: Short pointer to HOWTOGENIE.md
    - path: docs/howto-genie-setup-guide.md
      provides: Deprecated/redirect to HOWTOGENIE.md
    - path: CLAUDE.md
      provides: Pointer to docs/HOWTOGENIE.md for workflow list and config reference
  key_links:
    - from: docs/HOWTOGENIE.md
      to: "repo workflow paths (core/, content/, growth/, social/, affiliate/, monitoring/, email/)"
      via: "Workflows section table with actual JSON paths"
      pattern: "core/|content/|growth/|social/|affiliate/|monitoring/|email/"
    - from: docs/HOWTOGENIE.md
      to: htg_config.csv
      via: "Config Keys section (source of truth: htg_config.csv; table or summary in doc)"
      pattern: "htg_config\\.csv|Config Keys"
    - from: docs/ORCHESTRATOR-README.md
      to: docs/HOWTOGENIE.md
      via: "1–2 line pointer at top"
    - from: CLAUDE.md
      to: docs/HOWTOGENIE.md
      via: "Explicit pointer for workflow list and config key reference"
---

<objective>
Deliver one detailed, centralized Markdown doc as the single authoritative reference for workflows, UI, config keys, schedule, and archive; deprecate other human-facing setup docs with pointers; point CLAUDE.md to the doc so there are no parallel conflicting doc sets.
Purpose: Satisfy DOCS-01 and DOCS-02; enable future updates when adding workflows or config.
Output: docs/HOWTOGENIE.md (new), updated docs/ORCHESTRATOR-README.md, docs/howto-genie-setup-guide.md (redirect/deprecate), updated CLAUDE.md (pointer only).
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-docs-consolidation/07-CONTEXT.md
@.planning/phases/07-docs-consolidation/07-RESEARCH.md
@.planning/phases/07-docs-consolidation/07-VALIDATION.md

# Locked: CLAUDE.md stays separate; consolidated doc = human reference. No merging full CLAUDE content into the doc.
# Repo workflow layout: core/, content/, growth/, social/, affiliate/, monitoring/, email/, archive/ (actual JSON paths — not root-level names from CLAUDE.md).
# Config source of truth: htg_config.csv (79 keys); doc holds reference table or summary.
# Existing content to merge/adapt: docs/howto-genie-setup-guide.md (Overview, 8-agent pipeline, monetization, image, social, checklist, performance).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create consolidated doc (docs/HOWTOGENIE.md)</name>
  <files>docs/HOWTOGENIE.md</files>
  <action>
Create docs/HOWTOGENIE.md as the single authoritative reference. Use these exact top-level section headers so structural verification passes: ## Overview, ## Workflows, ## Config Keys, ## Schedule, ## UI, ## Archive.

1) **Overview** — Short system description (1–2 paragraphs). Reuse or adapt the system overview from docs/howto-genie-setup-guide.md (pipeline diagram / high-level flow). Do not copy long CLAUDE.md prose.

2) **Workflows** — One table by directory. Columns: Directory | Workflow file | Schedule/Trigger | Purpose. List only workflows that exist under core/, content/, growth/, social/, affiliate/, monitoring/, email/ (exclude archive/ from main table; mention archive in Archive section). Derive from actual repo: e.g. core/08_Orchestrator_v3.json, core/01_Config_Loader.json, core/Ollama Agent (Central).json, content/*.json, growth/*.json, social/*.json, affiliate/*.json, monitoring/*.json, email/*.json. Do not use root-level or outdated paths from CLAUDE.md.

3) **Config Keys** — State: "Source of truth: htg_config.csv." Add a reference table: Key | Description (or Purpose). Populate from htg_config.csv (key column); descriptions can be one-line or grouped (e.g. Google Sheets, WordPress, Ollama, Social, Feature flags). Optional: "When adding a new key, add a row to htg_config.csv and optionally add a row here."

4) **Schedule** — One section: global schedule (e.g. 8 AM Orchestrator, 10:30 Video, 12 Repurposing, etc.) as a single table or list. Can mirror ORCHESTRATOR-README schedule but use workflow filenames/paths from the Workflows section.

5) **UI** — Dashboards and Mission Control: adhd-mission-control.tsx, revenue-dashboard.tsx, Laravel Mission Control (laravel/). Short subsection each; where they live, what they show, how they connect (n8n webhooks, Google Sheets). Reference ui/ and laravel/ paths.

6) **Archive** — Short subsection: "Superseded or unused workflows and assets are in archive/. See archive/README.md (added in Phase 8) for what lives where." No need to list every archive file here.

7) Optional short subsections: "How to add a workflow" (add to Workflows table, ensure Execute Workflow callers updated), "How to add a config key" (add row to htg_config.csv, optionally to Config Keys table). Per RESEARCH, planner's discretion — include if it fits without bloating.

Merge or adapt content from docs/howto-genie-setup-guide.md where it fits (e.g. 8-agent roles table, image pipeline logic, monetization summary) into Overview or relevant sections. Do not duplicate CLAUDE.md conventions or full repository structure tree.
  </action>
  <verify>
    <automated>test -f docs/HOWTOGENIE.md &amp;&amp; grep -qE '^## (Overview|Workflows|Config Keys|Schedule|UI|Archive)' docs/HOWTOGENIE.md</automated>
  </verify>
  <done>docs/HOWTOGENIE.md exists with all six required sections; workflow list uses actual repo paths; config key reference cites htg_config.csv; schedule and UI documented; Archive section or pointer present.</done>
</task>

<task type="auto">
  <name>Task 2: Deprecate other docs and point CLAUDE to HOWTOGENIE</name>
  <files>docs/ORCHESTRATOR-README.md, docs/howto-genie-setup-guide.md, CLAUDE.md</files>
  <action>
Ensure the consolidated doc is the single authoritative reference; no parallel conflicting doc sets.

1) **docs/ORCHESTRATOR-README.md** — Add at the top (after or before the first heading) a 1–2 line pointer: e.g. "For the full workflow list, schedule, and config reference, see [HowTo-Genie reference](HOWTOGENIE.md)." Keep the rest of the file for orchestrator-specific details (Replace IDs, Schedule table) or trim to minimal plus link.

2) **docs/howto-genie-setup-guide.md** — Replace body with a short redirect so no one uses it as the main setup doc. Example: "This content has been consolidated into [HOWTOGENIE.md](HOWTOGENIE.md). Use that document as the single reference for setup, workflows, config, schedule, and archive." Optionally keep the first heading and the single paragraph + link. Do not leave the old long content in place as a competing reference.

3) **CLAUDE.md** — Add a clear pointer so workflow list and config key reference are not duplicated. In the "Repository Structure" section or in a new short "Canonical reference" subsection near the top, add one sentence: "For the canonical workflow list, schedule, and config key reference, see **docs/HOWTOGENIE.md**." Do not copy the workflow table or config table from HOWTOGENIE into CLAUDE.md. Optionally add a line under the "howto-genie-setup-guide.md" tree entry: "Deprecated; see docs/HOWTOGENIE.md."
  </action>
  <verify>
    <automated>grep -q 'HOWTOGENIE.md' docs/ORCHESTRATOR-README.md &amp;&amp; grep -q 'HOWTOGENIE.md' docs/howto-genie-setup-guide.md &amp;&amp; grep -q 'HOWTOGENIE.md' CLAUDE.md</automated>
  </verify>
  <done>ORCHESTRATOR-README and howto-genie-setup-guide point to HOWTOGENIE.md; CLAUDE.md contains explicit pointer to docs/HOWTOGENIE.md for workflow list and config; no duplicate long-form workflow/config content in CLAUDE.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify single authoritative reference (DOCS-02)</name>
  <files>(none — verification only)</files>
  <action>Executor pauses for human. Human performs the steps in how_to_verify below and replies with "approved" or with issues. No code or file changes in this task.</action>
  <verify>
    <automated>N/A — checkpoint; human confirms per how_to_verify</automated>
  </verify>
  <done>Human confirmed doc is single authoritative reference; no competing setup doc; workflow list and config reference present for updates.</done>
  <what_built>Consolidated doc at docs/HOWTOGENIE.md; other setup docs deprecated/redirected; CLAUDE.md points to HOWTOGENIE.md.</what_built>
  <how_to_verify>
1. Open docs/HOWTOGENIE.md — confirm it contains Workflows, Config Keys, Schedule, UI, Archive and is readable end-to-end.
2. Open docs/howto-genie-setup-guide.md — confirm it no longer contains the full setup content; it redirects to HOWTOGENIE.md.
3. Open docs/ORCHESTRATOR-README.md — confirm it links to HOWTOGENIE.md for full reference.
4. Open CLAUDE.md — confirm it points to docs/HOWTOGENIE.md for workflow list and config; no duplicate workflow/schedule table in CLAUDE.
5. Confirm no other file in the repo claims to be the "main" or "primary" setup reference for the same scope (workflows + config + schedule).
  </how_to_verify>
  <resume_signal>Type "approved" or describe any issue (e.g. competing doc, missing section, wrong paths).</resume_signal>
</task>

</tasks>

<verification>
- Structural: test -f docs/HOWTOGENIE.md && grep -qE '^## (Overview|Workflows|Config Keys|Schedule|UI|Archive)' docs/HOWTOGENIE.md
- Pointers: grep -q HOWTOGENIE.md in docs/ORCHESTRATOR-README.md, docs/howto-genie-setup-guide.md, CLAUDE.md
- DOCS-02: Manual — no parallel conflicting setup doc; workflow list and config reference present for updates (confirmed at checkpoint).
</verification>

<success_criteria>
- DOCS-01: One consolidated Markdown doc exists covering all workflows, UI, config keys, schedule, and archive.
- DOCS-02: Doc is the single authoritative reference; no parallel conflicting doc sets; doc includes workflow list and config key reference for future updates.
</success_criteria>

<output>
After completion, create .planning/phases/07-docs-consolidation/07-01-SUMMARY.md
</output>
