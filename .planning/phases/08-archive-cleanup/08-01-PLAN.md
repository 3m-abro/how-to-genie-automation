---
phase: 08-archive-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-archive-refs.sh
  - scripts/caller-audit.sh
  - .planning/phases/08-archive-cleanup/08-VALIDATION.md
autonomous: true
requirements:
  - ARCH-02
user_setup: []

must_haves:
  truths:
    - "No active workflow JSON references an archived workflow root id"
    - "Caller audit can list every file that references a given workflow id before move"
  artifacts:
    - path: scripts/verify-archive-refs.sh
      provides: "Assert no active-dir JSON contains root id of any workflow in archive/"
      min_lines: 20
    - path: scripts/caller-audit.sh
      provides: "Given workflow file path, output list of active-dir files that reference its root id"
      min_lines: 15
    - path: .planning/phases/08-archive-cleanup/08-VALIDATION.md
      provides: "Validation strategy; Wave 0 script reference"
  key_links:
    - from: scripts/verify-archive-refs.sh
      to: archive/*.json and core/ content/ growth/ social/ affiliate/ monitoring/ email/
      via: "Extract root id from each archive workflow; grep that id in active dirs; exit 1 if match"
      pattern: "grep.*workflowId|id"
    - from: scripts/caller-audit.sh
      to: "single workflow path"
      via: "Extract root id; grep id in active dirs; print matching files"
      pattern: "id.*grep"
---

<objective>
Add verification script and caller-audit script so Phase 8 can (1) assert no broken Execute Workflow references after moves and (2) discover every caller of a workflow before moving it.

Purpose: Satisfy ARCH-02 verification and support update-before-move workflow.
Output: scripts/verify-archive-refs.sh, scripts/caller-audit.sh; 08-VALIDATION.md updated to reference them.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-archive-cleanup/08-CONTEXT.md
@.planning/phases/08-archive-cleanup/08-RESEARCH.md
@.planning/phases/08-archive-cleanup/08-VALIDATION.md

Active dirs (no archive/): core/, content/, growth/, social/, affiliate/, monitoring/, email/.
n8n workflow JSON can have root-level "id" at end of file (e.g. "id": "I8PPRXOekRYyhUjl"). Execute Workflow nodes reference it as "workflowId": "<id>" or "workflowId": { "__rl": true, "value": "<id>", "mode": "id" }. Grep for the raw id string in active-dir JSON finds callers.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create verify-archive-refs script</name>
  <files>scripts/verify-archive-refs.sh</files>
  <action>
Create scripts/ directory if missing. Add scripts/verify-archive-refs.sh that:
1. For each JSON file under archive/ (e.g. find archive -name '*.json'), extract the root-level "id" from the JSON (use Node: require/readFileSync, parse, read w.id; if missing skip that file with a log to stderr).
2. For each non-empty id, grep -r that id in core/ content/ growth/ social/ affiliate/ monitoring/ email/ (--include='*.json' only). Do not search archive/.
3. If any file in those dirs contains the id, print the file path to stderr and exit 1.
4. If no matches for all archive ids, exit 0.
Use a small Node one-liner or inline script to extract id from each file; shell loop over archive JSONs and grep. Make script executable (chmod +x). Document in script comment: "Phase 8 — assert no active workflow references an archived workflow id (ARCH-02)."
  </action>
  <verify>
    <automated>Run from repo root: ./scripts/verify-archive-refs.sh; expect exit 0 (current state: archive workflows should not be referenced by active dirs). If any active file referenced an archive id, script would exit 1.</automated>
  </verify>
  <done>Script exists, executable; exits 0 when no active JSON references any archive workflow id; exits 1 and prints matching files otherwise.</done>
</task>

<task type="auto">
  <name>Task 2: Create caller-audit script and update VALIDATION</name>
  <files>scripts/caller-audit.sh, .planning/phases/08-archive-cleanup/08-VALIDATION.md</files>
  <action>
1. Add scripts/caller-audit.sh that accepts one argument: path to a workflow JSON file (e.g. content/02_Topic_Research_Engine_v2.json). Script (a) extracts root "id" from that JSON (Node or jq/grep); (b) greps that id in core/ content/ growth/ social/ affiliate/ monitoring/ email/ --include='*.json'; (c) prints each matching file path (one per line). If no root id, print to stderr and exit 1. Do not search archive/. Make executable.
2. Update .planning/phases/08-archive-cleanup/08-VALIDATION.md: set wave_0_complete to true when both scripts exist; in "Wave 0 Requirements" section mark the verify script and checklist items as done; add "Quick run command: scripts/verify-archive-refs.sh" and reference scripts/caller-audit.sh for caller discovery before moving a workflow.
  </action>
  <verify>
    <automated>Run scripts/caller-audit.sh with an active workflow that is called (e.g. core/01_Config_Loader.json or core/08_Orchestrator_v3.json). Script must output at least one caller path or exit 0 with no output if none. Run ./scripts/verify-archive-refs.sh; exit code 0.</automated>
  </verify>
  <done>caller-audit.sh exists and returns list of caller files for a given workflow path; 08-VALIDATION.md updated with Wave 0 complete and script references.</done>
</task>

</tasks>

<verification>
- ./scripts/verify-archive-refs.sh exits 0.
- scripts/caller-audit.sh path/to/workflow.json prints caller files or nothing.
</verification>

<success_criteria>
- Both scripts exist under scripts/, are executable, and behave as specified.
- 08-VALIDATION.md wave_0_complete true and script names documented.
</success_criteria>

<output>
After completion, create .planning/phases/08-archive-cleanup/08-01-SUMMARY.md
</output>
