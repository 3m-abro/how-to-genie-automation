---
phase: 08-archive-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - archive/README.md
  - docs/HOWTOGENIE.md
  - htg_config.csv
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
user_setup: []

must_haves:
  truths:
    - "Unused/superseded workflows in active dirs are moved to archive/ and no longer in core/content/growth/social/affiliate/monitoring/email/"
    - "No active workflow JSON references an archived workflow id (verify script passes)"
    - "Every file under archive/ is listed or described in archive/README.md"
    - "docs/HOWTOGENIE.md Archive section points to archive/README.md for what lives where"
  artifacts:
    - path: archive/README.md
      provides: "What lives in archive; grouped or flat list with brief reason per item"
      min_lines: 25
    - path: docs/HOWTOGENIE.md
      provides: "Archive section referencing archive/README.md"
  key_links:
    - from: archive/README.md
      to: archive/*.json (and any other archived assets)
      via: "List or describe each file; brief reason (e.g. Superseded by X)"
    - from: docs/HOWTOGENIE.md
      to: archive/README.md
      via: "Archive section: See archive/README.md (added in Phase 8) for what lives where"
      pattern: "archive/README"
---

<objective>
Identify workflow and asset archive candidates, update every Execute Workflow caller (and config if needed) before moving workflows to archive/, add archive/README.md, and ensure consolidated docs reflect the archive.

Purpose: Satisfy ARCH-01 (move unused/superseded to archive with README), ARCH-02 (no broken refs), ARCH-03 (document where things live).
Output: archive/README.md; any workflow moves from active dirs to archive/; caller and htg_config updates; HOWTOGENIE Archive section correct.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-archive-cleanup/08-CONTEXT.md
@.planning/phases/08-archive-cleanup/08-RESEARCH.md
@docs/HOWTOGENIE.md

Active workflows = HOWTOGENIE Workflows table (Directory + Workflow file). Add to active set any workflow whose root id is referenced by an in-table workflow (transitive: "called by"). Candidates = all .json under core/, content/, growth/, social/, affiliate/, monitoring/, email/ that are not in the active set. Before moving a workflow: run scripts/caller-audit.sh for that file; update every caller (replace workflowId with replacement workflow id or remove the Execute Workflow node); if htg_config.csv has CONTENT_CALENDAR_WF_ID or TOPIC_RESEARCH_WF_ID pointing to that workflow id, update or clear. Then move workflow to archive/. Run scripts/verify-archive-refs.sh after all moves; must pass. Never delete workflow or UI source files — archive only. archive/ already contains 11 workflow JSONs; README is missing and must be added.
</context>

<interfaces>
From docs/HOWTOGENIE.md — Workflows table (Directory | Workflow file):
- core/: 01_Config_Loader.json, 07_Approval_Poller.json, 08_Orchestrator_v3.json, Ollama Agent (Central).json
- content/: 02_Topic_Research_Engine_v2.json, 10_Content_Calendar_Manager.json, 12_Internal_Linking.json, 13_Content_Refresh.json, v3.0 — Content Repurposing Engine.json, v4.0 — SEO Interlinking Intelligence Engine.json
- growth/: 06_Refresh_Candidates_Writer.json, HowTo-Genie v4.0 — A_B Testing..., Competitor..., Islamic..., Multi-Language..., Viral Amplifier Queue, Viral Content Amplifier Engine, Voice & Audio..., WhatsApp & Telegram..., Video Production Engine
- social/: 04_Social_Formatter_v2.json, 06_Blotato_SubWorkflow.json, 11_Queue_Processor_v2.json, 14_Video_Production.json
- affiliate/: 06_Affiliate_Link_Manager.json, 10_Affiliate_Research_v2.json, 15_Affiliate_Link_Registry.json
- monitoring/: 09_Comment_Moderation.json, Alert_Handler.json, System_Health_Monitor.json
- email/: v3.0 — Email Newsletter Automation.json
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Identify candidates, update callers, move workflows</name>
  <files>core/*.json, content/*.json, growth/*.json, social/*.json, affiliate/*.json, monitoring/*.json, email/*.json, htg_config.csv, archive/</files>
  <action>
1. Build active set: workflow file paths from HOWTOGENIE Workflows table (docs/HOWTOGENIE.md). For each, get root "id" from JSON; find all .json in active dirs that contain that id (these are "callers"). The set of workflows that are either in the table or are the target of an Execute Workflow node in that set = active (transitive closure). Use scripts/caller-audit.sh to find callers for in-table workflows and build id→path map; active set = table paths + paths of workflows whose id is referenced by table workflows.
2. List all .json under core/, content/, growth/, social/, affiliate/, monitoring/, email/. Archive candidates = those not in active set.
3. For each candidate: (a) run scripts/caller-audit.sh path/to/candidate.json; (b) if callers found, edit each caller JSON to update the Execute Workflow node (point workflowId to the replacement workflow id if there is one, or remove the node if no replacement); (c) check htg_config.csv for CONTENT_CALENDAR_WF_ID, TOPIC_RESEARCH_WF_ID — if value equals this workflow's root id, update to correct active workflow id or leave empty; (d) move candidate to archive/.
4. Run scripts/verify-archive-refs.sh; must exit 0. If it fails, fix remaining callers or config and re-run until pass.
Use plain JavaScript in any one-off script (no new deps). Never delete workflow files — move only.
  </action>
  <verify>
    <automated>./scripts/verify-archive-refs.sh exits 0. No .json in core/ content/ growth/ social/ affiliate/ monitoring/ email/ that references the root id of any workflow file under archive/.</automated>
  </verify>
  <done>All archive candidates moved to archive/; every caller updated or node removed; config updated if needed; verify script passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add archive/README.md and update HOWTOGENIE</name>
  <files>archive/README.md, docs/HOWTOGENIE.md</files>
  <action>
1. Create archive/README.md. List or describe every file under archive/ (all workflow JSONs and any other archived assets). Use grouped list (e.g. "Orchestrators", "Topic Research", "Social", "Affiliate", "Video", "Other") with one-line reason per file (e.g. "Superseded by core/08_Orchestrator_v3.json", "Unused", "Superseded by content/02_Topic_Research_Engine_v2.json"). Match exact filenames under archive/.
2. Ensure docs/HOWTOGENIE.md Archive section (## Archive) states that superseded/unused workflows and assets are in archive/ and points to archive/README.md for what lives where. Current text: "See **archive/README.md** (added in Phase 8) for what lives where." If that is already present, confirm and leave as is; otherwise add or fix.
  </action>
  <verify>
    <automated>archive/README.md exists. grep -l "archive/README" docs/HOWTOGENIE.md returns the file. Every .json under archive/ is mentioned in archive/README.md (by name or group).</automated>
  </verify>
  <done>archive/README.md exists and lists/describes all archive contents with brief reason; HOWTOGENIE Archive section points to archive/README.md.</done>
</task>

</tasks>

<verification>
- scripts/verify-archive-refs.sh exits 0.
- archive/README.md exists; every file in archive/ referenced in README.
- docs/HOWTOGENIE.md contains Archive section pointing to archive/README.md.
</verification>

<success_criteria>
- ARCH-01: Unused/superseded workflows in archive/ with README.
- ARCH-02: No broken Execute Workflow refs (script passes).
- ARCH-03: Archive and "what lives where" documented (README + HOWTOGENIE).
</success_criteria>

<output>
After completion, create .planning/phases/08-archive-cleanup/08-02-SUMMARY.md
</output>
