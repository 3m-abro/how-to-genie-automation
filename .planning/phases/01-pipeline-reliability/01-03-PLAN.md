---
phase: 01-pipeline-reliability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - core/08_Orchestrator_v3.json
autonomous: true
requirements:
  - PIPE-03
user_setup: []

must_haves:
  truths:
    - "WordPress publish uses native n8n WordPress node with retry on fail"
    - "On publish failure, status publish_failed is set and satellites (Blotato, Calendar) do not run"
    - "Content Log receives status published or publish_failed from a single Code node after WP"
  artifacts:
    - path: core/08_Orchestrator_v3.json
      provides: "WordPress node (n8n-nodes-base.wordpress), Capture WP Post Data Code node, Publish Succeeded? IF, satellite gate"
  key_links:
    - from: "📝 Publish to WordPress"
      to: "native node"
      via: "type n8n-nodes-base.wordpress, retryOnFail true, maxTries 3, waitBetweenTries 5000"
    - from: "🔀 Publish Succeeded?"
      to: "🎨 Queue via Blotato / 📅 Write to Blog Calendar / 🚀 Trigger Content Calendar Manager"
      via: "true branch only; false branch writes publish_failed and Telegram"
---

<objective>
Satisfy PIPE-03: replace WordPress publish httpRequest with native WordPress node and retry; add Capture WP Post Data Code node and Publish Succeeded? IF so satellites run only when status !== publish_failed.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-reliability/01-CONTEXT.md
@.planning/phases/01-pipeline-reliability/01-RESEARCH.md
</context>

<interfaces>
Native WordPress node: resource "post", operation "create"; additionalFields: title, content, slug, status "publish", featuredMediaId (camelCase, number — RESEARCH.md), excerpt. Use featuredMediaId from Capture Media ID output (media_id). n8n retry limit: maxTries ≤ 5, waitBetweenTries ≤ 5000 ms; use maxTries 3, waitBetweenTries 5000. For 30s delay (user preference) document in summary that executor may add Wait+Loop if required.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace WP httpRequest with native WordPress node and add Capture WP Post Data</name>
  <files>core/08_Orchestrator_v3.json</files>
  <action>
    (1) Replace the "📝 Publish to WordPress" node: change type from httpRequest to n8n-nodes-base.wordpress, typeVersion 1. Set parameters.resource = "post", parameters.operation = "create". Map title, content, slug, status "publish", excerpt from "🔧 Assemble Final Article". Set additionalFields.featuredMediaId to "🔗 Capture Media ID".item.json.media_id (use 0 if missing). Add retryOnFail: true, maxTries: 3, waitBetweenTries: 5000 at node top level. Attach WordPress credential (wordpressApi). (2) Insert a new Code node "🔗 Capture WP Post Data" immediately after "📝 Publish to WordPress". In try: read $input.first().json from WP node (native node returns post with id, link); return [{ json: { id: $json.id, link: $json.link || $json.url, status: 'published' } }]. In catch: return [{ json: { id: '', link: '', status: 'publish_failed' } }]. This ensures downstream nodes always receive a consistent shape.
  </action>
  <verify>
    <automated>grep -q '"n8n-nodes-base.wordpress"' core/08_Orchestrator_v3.json && grep -q "retryOnFail" core/08_Orchestrator_v3.json && grep -q "Capture WP Post Data" core/08_Orchestrator_v3.json</automated>
  </verify>
  <done>WordPress publish is native node with retry; Capture WP Post Data normalizes success/failure to { id, link, status }.</done>
</task>

<task type="auto">
  <name>Task 2: Add Publish Succeeded? IF and gate satellite triggers</name>
  <files>core/08_Orchestrator_v3.json</files>
  <action>
    (1) Insert IF node "🔀 Publish Succeeded?" after "🔗 Capture WP Post Data". Condition: $json.status !== 'publish_failed' (or check $json.id or $json.link exists). (2) True branch: connect to the existing parallel nodes that currently run after WP publish — 🔍 Request Google Indexing, 🔍 Request Bing Indexing, 📡 Ping Sitemap, 🎨 Queue via Blotato, 📅 Write to Blog Calendar, 🔄 Update Calendar Status, 🚀 Trigger Content Calendar Manager, 📱 Telegram: Article Published. So all of these receive input only from the true branch. (3) False branch: connect to a path that will write a Content Log row with status publish_failed and send Telegram alert (the actual "Log publish_failed" and "Telegram: Publish Failed" nodes are wired in plan 01-05; here only ensure the IF false branch exists and does not connect to Blotato/Calendar/Telegram success). So: disconnect any direct connection from WP or Capture WP Post Data to Blotato/Calendar/Trigger/Telegram; route everything through Publish Succeeded? true. False branch output will be connected in 01-05 to Assemble Content Log Row and Telegram failure alert.
  </action>
  <verify>
    <automated>grep -q "Publish Succeeded" core/08_Orchestrator_v3.json && (grep -c "Queue via Blotato\|Write to Blog Calendar\|Trigger Content Calendar" core/08_Orchestrator_v3.json) -ge 1</automated>
  </verify>
  <done>Satellites and success Telegram run only when Publish Succeeded? is true; false branch exists for 01-05 to connect.</done>
</task>

</tasks>

<verification>
- WordPress node uses featuredMediaId (camelCase).
- No direct edge from WP or Capture node to Blotato/Calendar/Trigger without going through IF true.
</verification>

<success_criteria>
- PIPE-03: WP retries 3 times with 5s wait; publish_failed path exists and gates satellites.
</success_criteria>

<output>
After completion, create .planning/phases/01-pipeline-reliability/01-03-SUMMARY.md
</output>
