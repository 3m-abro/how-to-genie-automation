---
phase: 01-pipeline-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - htg_config.csv
  - .planning/phases/01-pipeline-reliability/01-VALIDATION.md
autonomous: true
requirements:
  - PIPE-01
  - PIPE-05
user_setup: []

must_haves:
  truths:
    - "Orchestrator runs at 8 AM daily without manual trigger"
    - "Config Loader runs before any agent and key values flow to nodes"
    - "REJECTED_POSTS_TAB is documented and present in repo config reference"
  artifacts:
    - path: htg_config.csv
      provides: "Config key REJECTED_POSTS_TAB for QC rejection tab name"
      contains: "REJECTED_POSTS_TAB"
    - path: .planning/phases/01-pipeline-reliability/01-VALIDATION.md
      provides: "Wave 0 instructions for n8n data table and Rejected Posts tab"
  key_links:
    - from: "core/08_Orchestrator_v3.json"
      to: "Config Loader"
      via: "executeWorkflow first after triggers"
      pattern: "Load Config|executeWorkflow"
---

<objective>
Verify PIPE-01 (8 AM daily, no manual intervention) and PIPE-05 (config at runtime). Add REJECTED_POSTS_TAB to repo config reference and document Wave 0 setup for Rejected Posts tab and n8n data table.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-reliability/01-CONTEXT.md
@.planning/phases/01-pipeline-reliability/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Config Loader position and 8 AM schedule</name>
  <files>core/08_Orchestrator_v3.json</files>
  <action>
    In core/08_Orchestrator_v3.json: (1) Confirm the Schedule Trigger node "🕗 Daily Trigger 8AM" has cronExpression "0 8 * * *". (2) Confirm "⚙️ Load Config" (executeWorkflow to Config Loader) is the first node after the two triggers (Daily Trigger and Entry Override) — i.e. all paths from triggers merge into Load Config before any "📡 Load Existing Topics" or "📡 Fetch Reddit Trending". (3) Document the result in a short note in this plan's summary or in 01-VALIDATION.md: "Config Loader verified as first node after triggers; schedule 0 8 * * *."
    Do not change orchestrator JSON unless the schedule is wrong; verification only.
  </action>
  <verify>
    <automated>grep -q '"0 8 \* \* \*"' core/08_Orchestrator_v3.json && grep -B2 -A2 "Load Config" core/08_Orchestrator_v3.json | head -20</automated>
  </verify>
  <done>Schedule is 8 AM daily; Config Loader is first after triggers; documented.</done>
</task>

<task type="auto">
  <name>Task 2: Add REJECTED_POSTS_TAB to htg_config.csv and document Wave 0</name>
  <files>htg_config.csv, .planning/phases/01-pipeline-reliability/01-VALIDATION.md</files>
  <action>
    (1) Add a row to htg_config.csv: key REJECTED_POSTS_TAB, value "Rejected Posts" (or the exact tab name the project uses). Match existing CSV format (e.g. key,value or key;value). (2) In 01-VALIDATION.md Wave 0 section, ensure it states: (a) Create "Rejected Posts" tab in Google Sheets with columns: date, topic, primary_keyword, qc_score, rejection_reasons, word_count, agent_fallbacks_used. (b) Add REJECTED_POSTS_TAB to the n8n htg_config data table with value "Rejected Posts" so the orchestrator can read it at runtime. Do not build any sync from CSV to n8n; CSV is documentation reference only.
  </action>
  <verify>
    <automated>grep -q "REJECTED_POSTS_TAB" htg_config.csv && grep -q "Rejected Posts" .planning/phases/01-pipeline-reliability/01-VALIDATION.md</automated>
  </verify>
  <done>htg_config.csv contains REJECTED_POSTS_TAB; VALIDATION.md Wave 0 lists Rejected Posts tab and n8n data table step.</done>
</task>

</tasks>

<verification>
- Config Loader is first in chain (manual check of 08_Orchestrator_v3.json connections).
- REJECTED_POSTS_TAB present in htg_config.csv.
- Wave 0 instructions clear for executor/user.
</verification>

<success_criteria>
- PIPE-01: Schedule and config order verified and documented.
- PIPE-05: Config runtime source confirmed; REJECTED_POSTS_TAB added to repo reference and Wave 0 documented.
</success_criteria>

<output>
After completion, create .planning/phases/01-pipeline-reliability/01-01-SUMMARY.md
</output>
