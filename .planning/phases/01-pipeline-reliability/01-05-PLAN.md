---
phase: 01-pipeline-reliability
plan: 05
type: execute
wave: 2
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - core/08_Orchestrator_v3.json
autonomous: true
requirements:
  - PIPE-02
  - PIPE-03
user_setup: []

must_haves:
  truths:
    - "Every run writes exactly one Content Log row with title, keyword, wp_url, status, parse_error, parse_error_agents"
    - "Success and publish_failed paths both funnel through Assemble Content Log Row before Log to Google Sheets"
    - "Parse & Validate Topic fallback sets parse_error true and success sets parse_error false"
  artifacts:
    - path: core/08_Orchestrator_v3.json
      provides: "📋 Assemble Content Log Row Code node, single path to 📊 Log to Google Sheets, Parse & Validate Topic parse_error field"
  key_links:
    - from: "📋 Assemble Content Log Row"
      to: "📊 Log to Google Sheets"
      via: "single incoming connection; all paths merge into Assemble first"
    - from: "🔀 Publish Succeeded? [false]"
      to: "📋 Assemble Content Log Row"
      via: "Code node receives Capture WP Post Data with status publish_failed"
    - from: "Publish success path"
      to: "📋 Assemble Content Log Row"
      via: "merge of indexing/Blotato/Calendar etc. so one item flows to Assemble"
---

<objective>
Satisfy PIPE-02 logging and PIPE-03 status in Sheets: add Assemble Content Log Row so all paths (success and publish_failed) write one machine-readable row with parse_error, parse_error_agents, status; add parse_error to Parse & Validate Topic; wire failure path to Assemble and Telegram Publish Failed.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-reliability/01-CONTEXT.md
@.planning/phases/01-pipeline-reliability/01-RESEARCH.md
</context>

<interfaces>
Capture WP Post Data (from 01-03): { id, link, status } where status is 'published' or 'publish_failed'. Parse & Validate QC: approved, average_score, parse_error. Assemble Final Article: title, content, slug, word_count, affiliate_ctas, parse_error. Parse & Validate Topic: topic, primary_keyword, status, _row, parse_error (to be added). Config: GOOGLE_SHEET_ID, CONTENT_LOG_TAB or equivalent for Content Log sheet/tab name.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add parse_error to Parse & Validate Topic and add Assemble Content Log Row</name>
  <files>core/08_Orchestrator_v3.json</files>
  <action>
    (1) In the "✅ Parse & Validate Topic" Code node: in the catch/fallback object add parse_error: true; in the success path add parse_error: false. Keep existing status: 'fallback_topic' for fallback. (2) Add Code node "📋 Assemble Content Log Row" that reads from: 🔧 Assemble Final Article, ✅ Parse & Validate Topic, ✅ Parse & Validate QC, 🔗 Capture WP Post Data, ⚙️ Load Config. Build parse_error_agents array: if Parse Prompt Package.parse_error then agent_0; if topic.parse_error or topic.status === 'fallback_topic' then agent_1; if article.parse_error then agent_4; if qc.parse_error then agent_5. WP: try/catch read Capture WP Post Data; wpUrl = wp.link || wp.url; wpId = wp.id; publishFailed = !wpUrl && (wp.status === 'publish_failed' or no id). Output single item: { date (YYYY-MM-DD), title, primary_keyword, wp_url, wp_post_id, word_count, status: publishFailed ? 'publish_failed' : 'published', parse_error: errorAgents.length > 0, parse_error_agents: JSON.stringify(errorAgents), qc_score, affiliate_ctas, run_timestamp }. Use 0 or '' for missing numbers/strings.
  </action>
  <verify>
    <automated>grep -q "Assemble Content Log Row" core/08_Orchestrator_v3.json && grep -q "parse_error" core/08_Orchestrator_v3.json</automated>
  </verify>
  <done>Parse & Validate Topic exposes parse_error; Assemble Content Log Row produces one row with all required fields.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all paths through Assemble to Log; connect publish_failed to Assemble and Telegram</name>
  <files>core/08_Orchestrator_v3.json</files>
  <action>
    (1) Disconnect all incoming connections to "📊 Log to Google Sheets". (2) Connect "📋 Assemble Content Log Row" as the only input to "📊 Log to Google Sheets". (3) Success path: ensure the merge of nodes after "🔀 Publish Succeeded? [true]" (Google Indexing, Bing Indexing, Ping Sitemap, Blotato, Calendar, Telegram success) eventually feeds into "📋 Assemble Content Log Row". Use a Merge node if needed so one item (from any of these) triggers Assemble; Assemble reads from named nodes ($('...')) so it does not depend on which branch sent the item. So: Publish Succeeded? [true] → [existing parallel nodes]; one of them or a Merge → Assemble Content Log Row → Log to Google Sheets. (4) Failure path: connect "🔀 Publish Succeeded? [false]" → "📋 Assemble Content Log Row" (same node). So both branches feed Assemble; Assemble reads Capture WP Post Data so it will see status publish_failed on the false branch. (5) Add "📱 Telegram: Publish Failed" (or reuse Telegram node with different message) on the false branch after Assemble → Log; send message e.g. "Publish failed: [title]. Check logs." Connect: Publish Succeeded? [false] → Assemble Content Log Row → Log to Google Sheets → Telegram: Publish Failed.
  </action>
  <verify>
    <automated>grep -q "Log to Google Sheets" core/08_Orchestrator_v3.json && (connections in JSON show Assemble Content Log Row → Log to Google Sheets)</automated>
  </verify>
  <done>Single Content Log row per run; success and failure paths both go through Assemble; publish_failed sends Telegram alert.</done>
</task>

</tasks>

<verification>
- No parallel path writes directly to Log to Google Sheets; all go through Assemble.
- Content Log row includes status, parse_error, parse_error_agents.
</verification>

<success_criteria>
- PIPE-02: parse_error and parse_error_agents in Content Log; PIPE-03: status published/publish_failed always written; failure path alerts via Telegram.
</success_criteria>

<output>
After completion, create .planning/phases/01-pipeline-reliability/01-05-SUMMARY.md
</output>
