---
phase: 11-voice-audio
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json
  - docs/HOWTOGENIE.md
autonomous: true
requirements:
  - VOICE-02
  - VOICE-04
user_setup: []

must_haves:
  truths:
    - "Workflow branches on VOICE_PROVIDER (local | elevenlabs | google); local calls TTS_SERVER_URL; cloud uses n8n credentials"
    - "One audio file per language row; files written to AUDIO_OUTPUT_PATH; log row appended to AUDIO_LOG_TAB"
    - "No YOUR_* in workflow JSON; schedule remains 16:00; GCS/Spotify/YouTube/RSS/Voice Note removed"
  artifacts:
    - path: growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json
      provides: "Voice workflow with local + cloud TTS, local file write, Audio Log append"
      contains: "Route by Voice Provider (3 branches), Process Audio (local binary), write to path, Log Audio"
  key_links:
    - from: "Configure Voice Settings"
      to: "config"
      via: "VOICE_PROVIDER from $('⚙️ Load Config').item.json"
      pattern: "VOICE_PROVIDER"
    - from: "Local TTS HTTP node"
      to: "config"
      via: "TTS_SERVER_URL"
      pattern: "TTS_SERVER_URL"
    - from: "Log Audio / write file"
      to: "config"
      via: "AUDIO_OUTPUT_PATH, AUDIO_LOG_TAB"
      pattern: "AUDIO_OUTPUT_PATH|AUDIO_LOG_TAB"
---

<objective>
Add VOICE_PROVIDER-based branching (local, elevenlabs, google), local TTS HTTP branch, replace hardcoded cloud API keys with n8n credentials, extend Process Audio for local binary, remove GCS/Spotify/YouTube/RSS/Voice Note, write audio to AUDIO_OUTPUT_PATH, append to AUDIO_LOG_TAB with idempotency, and document row shape and credential names in HOWTOGENIE.
</objective>

<execution_context>
Execute after 11-01. Run grep for YOUR_* after changes; 11-VALIDATION.md defines the check. Cloud TTS: use n8n credential references (e.g. xi-api-key for ElevenLabs, Google API key credential for Google TTS); document credential names in HOWTOGENIE.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-voice-audio/11-CONTEXT.md
@.planning/phases/11-voice-audio/11-RESEARCH.md
@.planning/phases/11-voice-audio/11-VALIDATION.md

Existing flow after 11-01: Trigger → Config Loader → Content Log → Filter today → IF no post → end; false → Multilingual read → Filter Multilingual by today → IF no rows → end; false → Configure Voice Settings → Fetch Article → LLM Adapt → Parse Audio Script → Route by Voice Provider (currently two branches: elevenlabs / google) → Process Audio → then GCS, RSS, Spotify, YouTube, Log. Per CONTEXT: local TTS = HTTP POST to TTS_SERVER_URL, body = tts_script, response = binary. Process Audio must handle three sources: ElevenLabs = binary; Google = base64 decode; local = HTTP response body binary. Audio Log: one row per language; idempotency key = post+date+language (e.g. slug+contentDate+language); before append, read existing AUDIO_LOG_TAB rows and skip append if row for same key exists. Row shape: Date, Language, Podcast Title, Duration, File Path (local path), Blog URL, Voice Provider, Status. Per-language failure: continue other languages; log failure in row or skip that language (planner choice: continue, append row with Status = Failed or omit row).
</context>

<tasks>

<task type="auto">
  <name>Task 1: VOICE_PROVIDER from config, three-way route, local TTS branch</name>
  <files>growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json</files>
  <action>
    - Update "🎛️ Configure Voice Settings" Code node: read VOICE_PROVIDER from $('⚙️ Load Config').item.json (local | elevenlabs | google). When VOICE_PROVIDER is "local", set voice_config.provider = 'local' and voice_config.voice_id or endpoint from config (e.g. use TTS_SERVER_URL for endpoint; voice_id for local can be language code or single default). Keep existing language→locale and voice_id map for elevenlabs/google; for local, provider is 'local' so Route sends to local branch.
    - Replace "🔀 Route by Voice Provider" with a three-way split: (1) local, (2) elevenlabs, (3) google. Use Switch node or two IF nodes: first IF voice_provider === 'local' → local branch; else second IF voice_provider === 'elevenlabs' → ElevenLabs; else → Google.
    - Add "🌐 Local TTS: Generate Audio" HTTP Request node: Method POST, URL = {{ ($('⚙️ Load Config').item.json.TTS_SERVER_URL || 'http://localhost:5000').replace(/\/$/, '') }}, Body Content Type Raw/Text, body = {{ $json.tts_script.substring(0, 5000) }}, Response Format = File (binary). Connect Route local branch → this node → merge with other TTS outputs before Process Audio.
    - Replace ElevenLabs and Google TTS nodes: remove hardcoded YOUR_ELEVENLABS_API_KEY and YOUR_GOOGLE_API_KEY. Use n8n credential references: for ElevenLabs use header "xi-api-key" from credential (name e.g. "ElevenLabs API" or "xi-api-key"); for Google TTS use credential for API key (e.g. "Google Cloud TTS" or query param from credential). Document credential names in HOWTOGENIE (Task 3).
  </action>
  <verify>
    <automated>grep -E 'YOUR_ELEVENLABS_API_KEY|YOUR_GOOGLE_API_KEY' "growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json" | wc -l; must be 0</automated>
  </verify>
  <done>VOICE_PROVIDER drives three branches; local branch calls TTS_SERVER_URL; cloud nodes use n8n credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Process Audio for local binary, write to AUDIO_OUTPUT_PATH, Audio Log with idempotency</name>
  <files>growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json</files>
  <action>
    - Remove nodes: "☁️ Upload to Google Cloud Storage", "📻 Build Podcast RSS Feed", "🎵 Submit to Spotify", "📺 Upload to YouTube", "💬 Create Voice Note Version", "🎤 Generate Voice Note Audio". Disconnect "🔊 Process Audio File" from GCS; connect Process Audio → new "Write audio to path" and "📊 Log Audio Content" only.
    - Update "🔊 Process Audio File" Code node: accept three input shapes—(1) ElevenLabs: binary in $input.first().binary or .data; (2) Google: $input.first().json.audioContent base64 decode; (3) local: binary from HTTP response (same as ElevenLabs shape). Unify to one output: json with audio_file_name, audio_format, etc., and binary.data. Build filename: slug_lang_timestamp (e.g. from scriptData or post identifier + language + Date.now()).
    - Add "Write audio to path" step: config AUDIO_OUTPUT_PATH from $('⚙️ Load Config').item.json; fullPath = AUDIO_OUTPUT_PATH + '/' + fileName (ensure trailing slash handling). Use n8n "Write Binary File" node if available with path = fullPath; otherwise Code node that receives binary and writes to fullPath (Node fs; document in HOWTOGENIE if n8n Code has no fs or Write Binary File missing). Output item must include fullPath and fileName for Log.
    - Update "📊 Log Audio Content" Google Sheets node: documentId and sheetName from config (GOOGLE_SHEET_ID, AUDIO_LOG_TAB). Columns: Date (content day), Language, Podcast Title, Duration, File Path (local fullPath), Blog URL, Voice Provider, Status. Before append: add Code node "Audio Log idempotency" that reads current AUDIO_LOG_TAB rows (from a prior Read node or from config + single read), builds key = postIdOrSlug + date + language; if row with same key exists, skip append (filter items so only new keys get passed to append). Alternatively: Read Audio Log tab once per run, in Code before append check existing keys and output only items whose key is not in existing; then append those. Document idempotency key (post+date+language) in HOWTOGENIE.
  </action>
  <verify>
    <automated>grep -E 'AUDIO_OUTPUT_PATH|AUDIO_LOG_TAB' "growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json" && ! grep -q 'Google Cloud Storage\|Spotify\|Upload to YouTube' "growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json"</automated>
  </verify>
  <done>Process Audio handles local + cloud; audio written to config path; log to config tab; idempotency prevents duplicate rows; cloud upload and Voice Note branches removed.</done>
</task>

<task type="auto">
  <name>Task 3: Document credentials, Audio Log row shape, idempotency, write path</name>
  <files>docs/HOWTOGENIE.md</files>
  <action>
    - In Config Keys (or new Voice subsection), ensure AUDIO_OUTPUT_PATH, AUDIO_LOG_TAB, TTS_SERVER_URL, MULTILINGUAL_CONTENT_TAB are listed (11-01 may have added some; add any missing).
    - Document Audio Log tab: one row per language; columns Date, Language, Podcast Title, Duration, File Path, Blog URL, Voice Provider, Status; idempotency key = post identifier + date (YYYY-MM-DD) + language—re-run same day skips duplicate append.
    - Document n8n credentials for Voice: ElevenLabs = credential for "xi-api-key" header (name e.g. "ElevenLabs API"); Google TTS = credential for API key (e.g. "Google Cloud TTS"). User must create these in n8n and add keys to credential.
    - Document that audio files are written to AUDIO_OUTPUT_PATH (local only this phase); if using Code node to write, ensure path is writable; if using Write Binary File node, document node name and path expression.
  </action>
  <verify>
    <automated>grep -E 'Audio Log|idempotency|xi-api-key|AUDIO_OUTPUT_PATH' docs/HOWTOGENIE.md | wc -l; must be >= 2</automated>
  </verify>
  <done>HOWTOGENIE documents Voice credentials, Audio Log row shape, idempotency, and local path.</done>
</tasks>

<verification>
- grep -E 'YOUR_|your-blog' "growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json" → no matches.
- Schedule trigger remains 0 16 * * * (4 PM).
- Manual: run workflow with VOICE_PROVIDER=local (and TTS server) or elevenlabs/google; verify one audio per language, file under AUDIO_OUTPUT_PATH, row in AUDIO_LOG_TAB.
</verification>

<success_criteria>
- VOICE_PROVIDER (local | elevenlabs | google) drives three TTS branches; local uses TTS_SERVER_URL; cloud uses n8n credentials.
- One audio per language; files in AUDIO_OUTPUT_PATH; log in AUDIO_LOG_TAB; idempotency by post+date+language.
- VOICE-02 and VOICE-04 satisfied.
</success_criteria>

<output>
After completion, create .planning/phases/11-voice-audio/11-02-SUMMARY.md
</output>
