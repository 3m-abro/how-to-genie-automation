---
phase: 11-voice-audio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json
  - docs/HOWTOGENIE.md
autonomous: true
requirements:
  - VOICE-01
  - VOICE-03
user_setup: []

must_haves:
  truths:
    - "Voice workflow runs Config Loader first; no node reads sheet before config"
    - "Today's post is selected from Content Log by date in owner timezone (CONTENT_DAY_TIMEZONE or TIMEZONE)"
    - "Multilingual Content rows are filtered by same today; when no rows, workflow outputs noMultilingualToday and ends without TTS"
    - "No YOUR_* or your-blog.com in workflow JSON for sheet/API keys used in this plan's nodes"
  artifacts:
    - path: growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json
      provides: "Voice workflow with Config Loader, timezone filter, empty-Multilingual branch"
      contains: "Execute Workflow (Config Loader), Filter today's post, Filter Multilingual by today"
    - path: docs/HOWTOGENIE.md
      provides: "Column contract (Multilingual + Audio Log) and config keys for Voice"
  key_links:
    - from: "Schedule Trigger"
      to: "Execute Workflow (Config Loader)"
      via: "first connection"
      pattern: "Load Config|Execute Workflow"
    - from: "Read Content Log node"
      to: "config"
      via: "documentId and sheetName from $('⚙️ Load Config').item.json"
      pattern: "GOOGLE_SHEET_ID|CONTENT_LOG_TAB"
    - from: "Read Multilingual Content node"
      to: "config"
      via: "MULTILINGUAL_CONTENT_TAB from config"
      pattern: "MULTILINGUAL_CONTENT_TAB"
---

<objective>
Add Config Loader at start, config-driven Content Log and Multilingual Content reads, timezone-aware "today" filters for both, empty-Multilingual branch, and document column contract and Voice config keys in HOWTOGENIE.
</objective>

<execution_context>
Execute plan tasks in order. After each task run grep for YOUR_* in the Voice workflow JSON to ensure no placeholders in modified nodes. Reference 11-VALIDATION.md for quick grep.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-voice-audio/11-CONTEXT.md
@.planning/phases/11-voice-audio/11-RESEARCH.md
@.planning/phases/11-voice-audio/11-VALIDATION.md

Config Loader is called via Execute Workflow (core/01_Config_Loader.json). Downstream nodes reference $('⚙️ Load Config').item.json; the Execute Workflow node must be named exactly "⚙️ Load Config". Timezone pattern: CONTENT_DAY_TIMEZONE || TIMEZONE from config; today = new Date().toLocaleDateString('en-CA', { timeZone: tz }). Use same today for both Content Log and Multilingual filters. Empty Multilingual behavior (per CONTEXT): when no rows for today, output single item { noMultilingualToday: true } and route to end (no TTS); do not implement English-only fallback this phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config Loader and Content Log with timezone filter</name>
  <files>growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json</files>
  <action>
    - Insert Execute Workflow node as first node after "🎙️ Audio Pipeline Trigger (4PM Daily)": call workflow core/01_Config_Loader.json; name the node exactly "⚙️ Load Config". Connect Trigger → ⚙️ Load Config.
    - Replace "📄 Get Today's Content" so it reads from config: documentId = {{ $('⚙️ Load Config').item.json.GOOGLE_SHEET_ID }}, sheetName = {{ $('⚙️ Load Config').item.json.CONTENT_LOG_TAB }}. Connect ⚙️ Load Config → 📄 Get Today's Content. Remove hardcoded YOUR_GOOGLE_SHEET_ID and "Content Log".
    - Add Code node "Filter today's post" after 📄 Get Today's Content. Get CONTENT_DAY_TIMEZONE || TIMEZONE from config; today = new Date().toLocaleDateString('en-CA', { timeZone: tz }); filter rows by date column (case-insensitive key: date/Date); if no matching row return [{ json: { noPostToday: true } }], else return single item { ...row, noPostToday: false } (pass through one row for downstream; Multilingual read needs today's post context only for merge—actually Multilingual is read separately). So: one row = today's post; zero rows = noPostToday true.
    - Add IF node "No post today?": condition noPostToday === true. True branch → end (no error). False branch → connect to next step (Read Multilingual Content).
  </action>
  <verify>
    <automated>grep -q "CONTENT_LOG_TAB\|Filter today's post" "growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json" && grep -q "⚙️ Load Config" "growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json"</automated>
  </verify>
  <done>Config Loader runs first; Content Log read uses config; Filter today's post uses timezone; no YOUR_* in Get Today's Content node.</done>
</task>

<task type="auto">
  <name>Task 2: Multilingual Content read, filter by today, empty branch</name>
  <files>growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json</files>
  <action>
    - Replace "🌍 Get All Language Versions" so it reads from config: documentId = {{ $('⚙️ Load Config').item.json.GOOGLE_SHEET_ID }}, sheetName = {{ $('⚙️ Load Config').item.json.MULTILINGUAL_CONTENT_TAB || 'Multilingual Content' }}. Connect "No post today?" false branch → this node. Remove hardcoded YOUR_GOOGLE_SHEET_ID and "Multilingual Content".
    - Add Code node "Filter Multilingual by today" after the Multilingual read. Use same timezone and today as in "Filter today's post" (from config). Filter rows by Date column (case-insensitive); if filtered.length === 0 return [{ json: { noMultilingualToday: true } }]; else return filtered.map(r => ({ json: r })) so each row is one item for the loop.
    - Add IF node "No Multilingual rows today?": condition noMultilingualToday === true. True branch → end (no TTS, no error). False branch → connect to "🎛️ Configure Voice Settings". So only when we have at least one Multilingual row do we proceed to Configure Voice Settings and TTS path.
  </action>
  <verify>
    <automated>grep -E 'noMultilingualToday|Filter Multilingual' "growth/HowTo-Genie v4.0 — Voice & Audio Content Pipeline.json" | wc -l; must be >= 1</automated>
  </verify>
  <done>Multilingual read uses MULTILINGUAL_CONTENT_TAB from config; Filter Multilingual by today uses same timezone; empty branch ends workflow without TTS.</done>
</task>

<task type="auto">
  <name>Task 3: Document column contract and Voice config keys</name>
  <files>docs/HOWTOGENIE.md</files>
  <action>
    - In Config Keys section, add Voice & Audio keys: VOICE_PROVIDER (local | elevenlabs | google), TTS_SERVER_URL (local TTS HTTP URL, e.g. http://localhost:5000), AUDIO_OUTPUT_PATH (local path for audio files), AUDIO_LOG_TAB (Sheet tab for audio log), MULTILINGUAL_CONTENT_TAB (Sheet tab for Multilingual Content; default "Multilingual Content"). Add CONTENT_DAY_TIMEZONE if not already documented (used for today filter).
    - Add a short "Column contract" subsection or row: Multilingual Content tab columns used by Voice: Date, Language, Translated Title, URL (case-insensitive lookup). Audio Log tab (AUDIO_LOG_TAB): one row per language; columns e.g. Date, Language, Podcast Title, Duration, File Path, Blog URL, Voice Provider, Status (document row shape for 11-02 to implement). Source: Multi-Language workflow writes Multilingual Content; Voice reads same columns.
  </action>
  <verify>
    <automated>grep -E 'AUDIO_LOG_TAB|MULTILINGUAL_CONTENT_TAB|TTS_SERVER_URL|VOICE_PROVIDER' docs/HOWTOGENIE.md | wc -l; must be >= 4</automated>
  </verify>
  <done>HOWTOGENIE documents Voice config keys and Multilingual + Audio Log column contract.</done>
</tasks>

<verification>
- Run grep from 11-VALIDATION.md: no YOUR_* in nodes modified in this plan (Content Log, Multilingual read).
- Manual (post-execution): Run workflow in n8n with TIMEZONE set; day with no Multilingual rows → noMultilingualToday true and workflow ends; day with rows → proceeds to Configure Voice Settings.
</verification>

<success_criteria>
- Config Loader is the first execution step after trigger.
- Content Log and Multilingual Content reads use config; today is computed in owner timezone for both filters.
- Empty Multilingual Content for today ends workflow without TTS.
- VOICE-01 (read today from both tabs, timezone-aware) and VOICE-03 (Config Loader first, empty branch, column contract doc) partially satisfied; 11-02 completes VOICE-02 and VOICE-04.
</success_criteria>

<output>
After completion, create .planning/phases/11-voice-audio/11-01-SUMMARY.md
</output>
