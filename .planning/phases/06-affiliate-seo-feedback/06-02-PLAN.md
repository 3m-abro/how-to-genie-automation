---
phase: 06-affiliate-seo-feedback
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - affiliate/06_Affiliate_Link_Manager.json
autonomous: true
requirements:
  - AFF-01
  - AFF-02
user_setup: []

must_haves:
  truths:
    - "Affiliate Link Manager workflow runs on a schedule (e.g. Monday 7 AM) and is gated by AFFILIATE_MANAGER_ENABLED"
    - "Workflow fetches Muncheye RSS (and optionally CBEngine RSS when config provides URL) and parses product data"
    - "Workflow writes/updates the affiliate product registry tab (AFFILIATE_REGISTRY_TAB) with deduped rows per product"
  artifacts:
    - path: affiliate/06_Affiliate_Link_Manager.json
      provides: "Scheduled Affiliate Link Manager workflow"
      contains: "Schedule Trigger, Execute Workflow (Config Loader), HTTP Request (RSS), Code parse, Google Sheets append/update"
  key_links:
    - from: "affiliate/06_Affiliate_Link_Manager.json"
      to: "Config Loader"
      via: "Execute Workflow node first; reference GOOGLE_SHEET_ID, AFFILIATE_REGISTRY_TAB, AFFILIATE_MANAGER_ENABLED"
      pattern: "Load Config|AFFILIATE_REGISTRY_TAB"
    - from: "affiliate/06_Affiliate_Link_Manager.json"
      to: "Google Sheets"
      via: "documentId and sheetName from config; write rows matching 06-CONFIG-KEYS registry shape"
      pattern: "googleSheets|AFFILIATE_REGISTRY"
---

<objective>
Create the Affiliate Link Manager n8n workflow that runs on schedule, reads config (Plan 01), fetches Muncheye RSS (and optional CBEngine RSS), parses and dedupes products, and writes to the affiliate product registry tab. Satisfies AFF-02 and completes AFF-01 (registry populated).
Purpose: Automatic refresh of the product registry from external feeds so Agent 1/4 have current, niche-relevant products.
Output: affiliate/06_Affiliate_Link_Manager.json (new workflow).
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-affiliate-seo-feedback/06-CONTEXT.md
@.planning/phases/06-affiliate-seo-feedback/06-RESEARCH.md
@.planning/phases/06-affiliate-seo-feedback/06-CONFIG-KEYS.md
affiliate/10_Affiliate_Research_v2.json
core/01_Config_Loader.json
</context>

<interfaces>
Registry row shape (from 06-CONFIG-KEYS.md): product_name, platform, commission, url, niche, score, date_found, status. Niches: productivity, finance, home, health, tech (or NICHES config). Config keys: GOOGLE_SHEET_ID (or SPREADSHEET_ID), AFFILIATE_REGISTRY_TAB, AFFILIATE_MANAGER_ENABLED. No YOUR_* in workflow JSON; all sheet IDs and tab names from config.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create workflow with Config Loader and RSS fetch</name>
  <files>affiliate/06_Affiliate_Link_Manager.json</files>
  <action>
Create new n8n workflow JSON: name "Affiliate Link Manager" (or "06 — Affiliate Link Manager").

1. **Schedule Trigger:** e.g. "0 7 * * 1" (Monday 7 AM) per RESEARCH; document in node notes.

2. **Execute Workflow — Config Loader:** Call core/01_Config_Loader (same pattern as 10_Affiliate_Research_v2). Use workflowId reference (REPLACE_WITH_CONFIG_LOADER_ID or id from project). Output: config object.

3. **IF — AFFILIATE_MANAGER_ENABLED:** Check config.AFFILIATE_MANAGER_ENABLED === 'true'. True branch continues; false branch ends workflow (no error).

4. **HTTP Request — Muncheye RSS:** GET Muncheye category RSS URL (e.g. https://muncheye.com/category/affiliate/other/feed or similar from RESEARCH). Accept XML; timeout 20000. User-Agent header: "Mozilla/5.0 (compatible; HowToGenie/1.0)".

5. **Code — Parse Muncheye RSS:** Parse XML response (item elements: title, link, description). Map to registry row shape: product_name (title), url (link), platform "Muncheye", niche (infer from description or default "productivity"), commission "", score 0, date_found today ISO date, status "active". Output array of items { json: { product_name, platform, commission, url, niche, score, date_found, status } }. Plain JavaScript only; no external libs; handle parse errors with fallback empty array.

Optional (if config has CBENGINE_RSS_URL or similar): second HTTP Request + Code parse for CBEngine; merge items. RESEARCH: CBEngine parameterized RSS may require Pro; document in workflow notes. If not adding CBEngine, single source (Muncheye) is sufficient for AFF-02.

Do not hardcode YOUR_GOOGLE_SHEET_ID or any secret. Reference config via $('Load Config').item.json or the actual Config Loader node name used.
  </action>
  <verify>
    <automated>test -f affiliate/06_Affiliate_Link_Manager.json && node -e "const w=require('./affiliate/06_Affiliate_Link_Manager.json'); console.log(w.nodes&&w.nodes.length>=4&&w.nodes.some(n=>/schedule|Schedule/i.test(n.name||''))?1:0)"</automated>
  </verify>
  <done>Workflow has Schedule, Config Loader, IF enabled, HTTP Muncheye RSS, and Code parse node; no YOUR_* in file.</done>
</task>

<task type="auto">
  <name>Task 2: Add dedupe/score and write to registry tab</name>
  <files>affiliate/06_Affiliate_Link_Manager.json</files>
  <action>
In the same workflow file (06_Affiliate_Link_Manager.json):

1. **Code — Dedupe by URL/product key:** After parse, dedupe items by url (or product_name+url). Optional: simple Ollama scoring node (JSON-only prompt: score 1-10 relevance for how-to content; temperature 0.4) to set score field; if skipped, leave score 0. Per RESEARCH scoring is optional.

2. **Google Sheets — Append (or Update):** Use documentId from config: $('Load Config').item.json.GOOGLE_SHEET_ID || $('Load Config').item.json.SPREADSHEET_ID. sheetName: $('Load Config').item.json.AFFILIATE_REGISTRY_TAB || 'Affiliate Registry'. Operation: append (appendOrUpdate if n8n supports dedupe by column; otherwise append). Map each item to columns matching 06-CONFIG-KEYS: product_name, platform, commission, url, niche, score, date_found, status. Ensure header row exists (first run may need to create sheet with headers) or document that user creates tab with header row.

3. **Error handling:** If HTTP or Sheets fails, do not throw uncaught; route to a node that returns a single item with success: false, error: { code, message } so the run is traceable.

Follow .cursor/rules/n8n-rule.mdc: no console.log, return array of { json } from Code nodes; no secrets in JSON.
  </action>
  <verify>
    <automated>grep -q "googleSheets\|googleSheets" affiliate/06_Affiliate_Link_Manager.json && grep -q "AFFILIATE_REGISTRY_TAB\|sheetName" affiliate/06_Affiliate_Link_Manager.json</automated>
  </verify>
  <done>Workflow writes parsed, deduped rows to the registry tab using config for sheet ID and tab name; AFF-02 satisfied.</done>
</task>

</tasks>

<verification>
- Manual: Run workflow (trigger or execute); confirm registry tab receives new rows with correct columns.
- No YOUR_* or hardcoded sheet ID in 06_Affiliate_Link_Manager.json.
</verification>

<success_criteria>
- Affiliate Link Manager runs on schedule, is enabled via config, fetches Muncheye RSS, parses and writes to AFFILIATE_REGISTRY_TAB; registry can hold at least one product per niche after run(s).
</success_criteria>

<output>
After completion, create .planning/phases/06-affiliate-seo-feedback/06-02-SUMMARY.md
</output>
