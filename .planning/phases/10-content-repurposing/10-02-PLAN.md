---
phase: 10-content-repurposing
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - content/v3.0 — Content Repurposing Engine.json
  - htg_config.csv
  - docs/HOWTOGENIE.md
autonomous: true
requirements:
  - REP-01
  - REP-02
user_setup: []

must_haves:
  truths:
    - "Only formats listed in REPURPOSE_FORMATS run (3–5); adding a format later is a config change"
    - "HTML is stripped before LLM; each format has LLM → Parse & Validate (success/data/error envelope)"
    - "Repurposed Content tab and optional queue tabs use config-driven names; one row per post/date"
  artifacts:
    - path: content/v3.0 — Content Repurposing Engine.json
      provides: "Config-driven format branches, Parse & Validate per LLM, Assemble and log"
    - path: htg_config.csv
      provides: "REPURPOSE_FORMATS, REPURPOSED_CONTENT_TAB, optional queue tab keys"
    - path: docs/HOWTOGENIE.md
      provides: "Config keys table and Repurposed Content column contract"
  key_links:
    - from: "Build format list Code node"
      to: "config"
      via: "REPURPOSE_FORMATS from $('⚙️ Load Config').item.json"
      pattern: "REPURPOSE_FORMATS"
    - from: "Each LLM node"
      to: "Parse & Validate Code node"
      via: "immediate successor"
      pattern: "success.*data.*error"

---

<objective>
Gate format execution on REPURPOSE_FORMATS, add Parse & Validate after each LLM, refactor Assemble to 3–5 formats and log to config-driven tabs; add config keys and document column contract in docs.
</objective>

<execution_context>
Plan 10-01 must be complete (Config Loader, timezone filter, idempotency in place). Execute tasks in order. Preserve "🧹 Clean & Extract Article" and WordPress fetch from config.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-content-repurposing/10-CONTEXT.md
@.planning/phases/10-content-repurposing/10-RESEARCH.md
@.planning/phases/10-content-repurposing/10-VALIDATION.md
@.cursor/rules/n8n-json-contracts.mdc
@.cursor/rules/ollama-json-only.mdc

Existing nodes: "🧹 Clean & Extract Article", "🐦 AI: Create Twitter Thread", "💼 AI: Create LinkedIn Article", "📸 AI: Create IG Carousel", "🎙️ AI: Create Podcast Script", "🌐 AI: Create Community Content", "📦 Assemble 10 Content Assets", "📊 Log to Repurposed Content Sheet", "🐦 Save Twitter Thread Queue", "🎙️ Save Podcast Queue". Format IDs: twitter, linkedin, ig_carousel, podcast, community.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config-driven formats, Parse & Validate, Assemble and log</name>
  <files>content/v3.0 — Content Repurposing Engine.json</files>
  <action>
    - Add Code node "Build format list" after "🧹 Clean & Extract Article": read REPURPOSE_FORMATS from $('⚙️ Load Config').item.json; default "twitter,linkedin,ig_carousel,podcast,community"; split by comma, trim, filter empty; output one item per format (e.g. [{ json: { format: 'twitter' } }, ...]) for Switch or branch routing. Per RESEARCH Pattern 3.
    - Gate execution so only formats in REPURPOSE_FORMATS run: use Switch node on format value (twitter, linkedin, ig_carousel, podcast, community) or equivalent (e.g. one branch per format with IF/route). Each branch runs one LLM node then one "Parse & Validate [Format]" Code node. Parse & Validate: parse LLM response (strip markdown/code fence), enforce success/data/error envelope; on parse error return { success: false, data: null, error: { code: 'parse_error', message } }. Per RESEARCH "Parse & Validate after LLM (envelope)".
    - Ensure each of the 5 LLM nodes (Twitter, LinkedIn, IG Carousel, Podcast, Community) is preceded by same Clean & Extract output and is followed by a Parse & Validate node. If using parallel branches, Merge node waits for all enabled branches then feeds Assemble.
    - Refactor "📦 Assemble 10 Content Assets" into "📦 Assemble repurposed payload": build one row per post with columns Source URL, Date (content day), format outputs (e.g. twitter_text, linkedin_text, ig_carousel_json, podcast_script, community_text or similar), Timestamp. Use $('Filter today\'s post').item.json for slug/date; collect format outputs from Parse & Validate nodes by node name. Column contract: at least Source URL, Date, Timestamp, and one column per enabled format (or single "Formats" JSON column). Document in node note or comment.
    - Update "📊 Log to Repurposed Content Sheet": documentId = config GOOGLE_SHEET_ID, sheetName = config REPURPOSED_CONTENT_TAB. Map Assemble output to columns per contract.
    - Update "🐦 Save Twitter Thread Queue" and "🎙️ Save Podcast Queue": documentId from config; sheetName from config TWITTER_QUEUE_TAB and PODCAST_QUEUE_TAB. If config key missing, skip that append (optional queue tabs). Use IF or only connect when key exists in config (executor may use expression that yields empty sheet name to skip, or omit nodes if not implementing optional queues for Phase 10 — planner choice: implement optional queues with config keys; if key missing, do not append to that tab).
  </action>
  <verify>
    <automated>grep -E 'YOUR_|your-blog' "content/v3.0 — Content Repurposing Engine.json" | wc -l; must be 0</automated>
  </verify>
  <done>REPURPOSE_FORMATS drives which formats run; each LLM has Parse & Validate; Assemble produces one row per post; Repurposed Content and optional queues use config tab names.</done>
</task>

<task type="auto">
  <name>Task 2: Config keys and docs</name>
  <files>htg_config.csv, docs/HOWTOGENIE.md</files>
  <action>
    - Add to htg_config.csv (key,value): REPURPOSE_FORMATS,"twitter,linkedin,ig_carousel,podcast,community"; REPURPOSED_CONTENT_TAB,Repurposed Content; TWITTER_QUEUE_TAB,Twitter Queue; PODCAST_QUEUE_TAB,Podcast Queue. Use CONTENT_DAY_TIMEZONE only if already present in config; otherwise TIMEZONE is used by 10-01 Filter.
    - In docs/HOWTOGENIE.md Config Keys table, add a row for Repurposing: REPURPOSE_FORMATS, REPURPOSED_CONTENT_TAB, TWITTER_QUEUE_TAB, PODCAST_QUEUE_TAB — purpose "repurposing formats and sheet tabs". Add brief column contract for Repurposed Content tab: Source URL, Date (content day), per-format columns or Formats JSON, Timestamp.
  </action>
  <verify>
    <automated>grep -E 'REPURPOSE_FORMATS|REPURPOSED_CONTENT_TAB' htg_config.csv docs/HOWTOGENIE.md | wc -l; must be >= 2</automated>
  </verify>
  <done>htg_config.csv contains repurposing keys; HOWTOGENIE.md documents them and Repurposed Content column contract.</done>
</task>

</tasks>

<verification>
- Quick grep: no YOUR_* or your-blog in workflow.
- REP-01: With REPURPOSE_FORMATS set to 3 formats, only 3 format outputs; with 5, all 5 run.
- REP-02: Clean & Extract strips HTML; each format has Parse & Validate; tabs from config.
</verification>

<success_criteria>
- 3–5 platform-native formats produced from config-driven set; HTML stripped, LLM per format with validated JSON; logging to config-driven tabs; config keys and column contract in docs.
</success_criteria>

<output>
After completion, create .planning/phases/10-content-repurposing/10-02-SUMMARY.md
</output>
