---
phase: 10-content-repurposing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/v3.0 — Content Repurposing Engine.json
autonomous: true
requirements:
  - REP-01
  - REP-03
  - REP-04
user_setup: []

must_haves:
  truths:
    - "Workflow runs Config Loader first; no node reads sheet/URL before config"
    - "Today's post is selected by date in owner timezone (CONTENT_DAY_TIMEZONE or TIMEZONE)"
    - "When no post today, workflow ends without error and does not append"
    - "When already repurposed (slug+date in Repurposed Content tab), workflow skips append"
    - "No YOUR_* or your-blog.com in workflow JSON; schedule is 0 12 * * *"
  artifacts:
    - path: content/v3.0 — Content Repurposing Engine.json
      provides: "Repurposing workflow with Config Loader, timezone filter, idempotency"
      contains: "Execute Workflow (Config Loader), Filter today's post, Already repurposed"
  key_links:
    - from: "Schedule Trigger"
      to: "Execute Workflow (Config Loader)"
      via: "first connection"
      pattern: "Load Config|Execute Workflow"
    - from: "Read Content Log node"
      to: "config"
      via: "documentId and sheetName from $('⚙️ Load Config').item.json"
      pattern: "GOOGLE_SHEET_ID|CONTENT_LOG_TAB"
    - from: "Fetch Full Article URL"
      to: "config"
      via: "WORDPRESS_URL from config"
      pattern: "WORDPRESS_URL"
---

<objective>
Add Config Loader at start, timezone-aware "today" filter, idempotency check, and remove all YOUR_* / your-blog.com placeholders so the repurposing workflow is config-first and safe to re-run.
</objective>

<execution_context>
Execute plan tasks in order. After each task run the quick grep from 10-VALIDATION.md to ensure no placeholders reintroduced.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-content-repurposing/10-CONTEXT.md
@.planning/phases/10-content-repurposing/10-RESEARCH.md
@.planning/phases/10-content-repurposing/10-VALIDATION.md

Reference patterns: RESEARCH.md "Pattern 1: Timezone-aware today", "Pattern 2: Idempotency check". Config Loader is called via Execute Workflow (core/01_Config_Loader.json); downstream nodes reference $('⚙️ Load Config').item.json (node name must be exactly "⚙️ Load Config").
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config Loader and timezone-aware today filter</name>
  <files>content/v3.0 — Content Repurposing Engine.json</files>
  <action>
    - Insert Execute Workflow node as first node after "⚙️ Repurposing Trigger (Noon Daily)": call workflow core/01_Config_Loader.json; name the node exactly "⚙️ Load Config". Connect Trigger → ⚙️ Load Config.
    - Replace "📄 Get Today's Post Data" so it reads from config: documentId = {{ $('⚙️ Load Config').item.json.GOOGLE_SHEET_ID }}, sheetName = {{ $('⚙️ Load Config').item.json.CONTENT_LOG_TAB }}. Connect ⚙️ Load Config → 📄 Get Today's Post Data.
    - Add Code node "Filter today's post" after 📄 Get Today's Post Data. Implement timezone-aware filter: get CONTENT_DAY_TIMEZONE || TIMEZONE from config; today = new Date().toLocaleDateString('en-CA', { timeZone }); filter rows by date column (match today) and status !== 'publish_failed'; detect slug from WP URL column (last path segment). If no matching row, return [{ json: { noPostToday: true } }]. Otherwise return single item { ...latestRow, slug, noPostToday: false }. Use robust column detection (date/Date, status/Status, url/WP URL) per RESEARCH Pattern 1.
    - Add IF node "No post today?": condition noPostToday === true. True branch → end (no error). False branch → continue to next step (later: idempotency then fetch).
    - Replace "📥 Fetch Full Article" URL with expression: {{ ($('⚙️ Load Config').item.json.WORDPRESS_URL || '').replace(/\/$/, '') }}/wp-json/wp/v2/posts?slug={{ $json.slug }}. Ensure this node runs only after "Filter today's post" false branch (and after idempotency check).
  </action>
  <verify>
    <automated>grep -E 'YOUR_GOOGLE_SHEET_ID|your-blog\.com' "content/v3.0 — Content Repurposing Engine.json" | wc -l; must be 0</automated>
  </verify>
  <done>Config Loader runs first; Content Log read uses config; Filter today's post uses timezone; Fetch uses WORDPRESS_URL from config; no YOUR_* or your-blog.com in file.</done>
</task>

<task type="auto">
  <name>Task 2: Idempotency check before append path</name>
  <files>content/v3.0 — Content Repurposing Engine.json</files>
  <action>
    - After "No post today?" false branch, add Google Sheets "Read Repurposed Content" node: documentId and sheetName from config. Use REPURPOSED_CONTENT_TAB from config (default "Repurposed Content" if key missing in plan; executor may use literal "Repurposed Content" until 10-02 adds key to htg_config). So sheetName = {{ $('⚙️ Load Config').item.json.REPURPOSED_CONTENT_TAB || 'Repurposed Content' }}.
    - Add Code node "Already repurposed?" that reads Repurposed Content rows from input and the current post from $('Filter today\'s post').item.json (or exact node name for Filter node). Build idempotency key as slug + '|' + contentDate (YYYY-MM-DD from filter row or today in config TZ). Check if any row in Repurposed Content has same key (compare Source URL slug + Date column). Return [{ json: { alreadyRepurposed: true|false, slug, contentDate } }]. Per RESEARCH Pattern 2.
    - Add IF node "Already repurposed?": condition alreadyRepurposed === true. True branch → end (no append). False branch → connect to "📥 Fetch Full Article" (so fetch runs only when we have a post and have not already repurposed it).
    - Ensure "📊 Log to Repurposed Content Sheet" and any queue appends are only reachable when alreadyRepurposed is false (they stay downstream of the main flow after Assemble).
  </action>
  <verify>
    <automated>grep -c "alreadyRepurposed\|Already repurposed" "content/v3.0 — Content Repurposing Engine.json"; must be >= 1</automated>
  </verify>
  <done>Repurposed Content tab is read before any append; slug+date check gates the fetch and LLM path; second run same day skips append.</done>
</task>

</tasks>

<verification>
- Run grep from 10-VALIDATION.md: no YOUR_* or your-blog.com in workflow JSON.
- Manually (post-execution): Run workflow in n8n with TIMEZONE set; day with one post → filter yields that row; day with no post → noPostToday true and workflow ends.
</verification>

<success_criteria>
- Config Loader is the first execution step after trigger.
- Content Log and WordPress URL come from config.
- Today is computed in owner timezone; idempotency prevents duplicate rows on re-run.
- REP-01 (read today), REP-03 (Config Loader, idempotent), REP-04 (Noon, no placeholders) satisfied by this plan.
</success_criteria>

<output>
After completion, create .planning/phases/10-content-repurposing/10-01-SUMMARY.md
</output>
