---
phase: 05-live-dashboards-monitoring
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - laravel/
  - ui/revenue-dashboard.tsx
autonomous: true
requirements:
  - DASH-01
user_setup:
  - service: google
    why: "Sheets API for revenue and content log data"
    env_vars:
      - name: GOOGLE_SHEET_ID
        source: "Sheet URL (spreadsheet ID)"
      - name: GOOGLE_APPLICATION_CREDENTIALS or service account JSON
        source: "Google Cloud Console — service account key"
    dashboard_config:
      - task: "Enable Google Sheets API; create service account; share sheet with service account email"
must_haves:
  truths:
    - "Revenue dashboard shows real post counts, traffic estimates, and affiliate data from Sheets"
  artifacts:
    - path: laravel/app/Services/GoogleSheetsService.php
      provides: "Read Content Log and Revenue Tracker tabs"
    - path: laravel/routes/api.php or web.php
      provides: "GET /api/dashboard/revenue"
    - path: ui/revenue-dashboard.tsx
      provides: "Dashboard component consuming live API data"
  key_links:
    - from: ui/revenue-dashboard.tsx
      to: "/api/dashboard/revenue"
      via: "fetch or useEffect"
      pattern: "fetch.*dashboard/revenue|api/dashboard/revenue"
    - from: "Revenue API route"
      to: "GoogleSheetsService"
      via: "controller or inline"
      pattern: "GoogleSheetsService|readRange|content_log|revenue_tracker"
---

<objective>
Bootstrap the Laravel app from the reference file, add config and Wave 0 test scaffold, implement Google Sheets–backed revenue API, and wire the Revenue dashboard UI to live data so the owner sees real post counts, traffic estimates, and affiliate data (DASH-01).
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-live-dashboards-monitoring/05-CONTEXT.md
@.planning/phases/05-live-dashboards-monitoring/05-RESEARCH.md
@ui/laravel-admin-panel.php
@ui/revenue-dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Laravel app, config, and Wave 0 tests</name>
  <files>laravel/, .env.example (or laravel/.env.example)</files>
  <action>
    Extract or create Laravel app from ui/laravel-admin-panel.php into a laravel/ directory (routes, app/Http/Controllers, app/Models, config, etc.). If the repo keeps the app elsewhere, document "apply to existing Laravel app" and use paths that map 1:1.
    Add config/services.php entries: n8n (base_url, api_key), telegram (bot_token, chat_id), google (sheet_id, content_log_tab, revenue_tracker_tab), weekly_summary_day (e.g. 1 for Monday, 7 for Sunday).
    Add .env.example with N8N_BASE_URL, N8N_API_KEY, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID, GOOGLE_SHEET_ID (and GOOGLE_APPLICATION_CREDENTIALS path or inline JSON if using google/apiclient). No secrets in repo.
    Create Wave 0 test stubs so every plan has an automated verify target: tests/Feature/Dashboard/RevenueDashboardApiTest.php (assert GET /api/dashboard/revenue returns 200 and response has expected keys, e.g. content_stats or revenue_data), tests/Feature/Dashboard/MissionControlApiTest.php (assert GET /api/n8n/status or mission-control returns 200 and has modules/system_status), tests/Feature/Commands/N8nFailureMonitorCommandTest.php (stub: command exists and runs without throwing), tests/Feature/Commands/WeeklySummaryCommandTest.php (stub: command exists and runs without throwing). Use Laravel HTTP test or artisan command test. Stubs may fail until implementation; implement only Revenue API in Plan 01 so RevenueDashboard filter passes after Task 2.
  </action>
  <verify>
    <automated>cd laravel && php artisan test --filter=RevenueDashboard 2>/dev/null || php artisan test 2>/dev/null; exit 0</automated>
  </verify>
  <done>Laravel app runnable with config and env.example; test scaffold present; Revenue test passes after Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: GoogleSheetsService and GET /api/dashboard/revenue</name>
  <files>laravel/app/Services/GoogleSheetsService.php, laravel/app/Http/Controllers/DashboardController.php or MissionControlController, laravel/routes/api.php or web.php</files>
  <action>
    Create GoogleSheetsService that reads Google Sheets via google/apiclient or revolution/laravel-google-sheets (per RESEARCH: do not hand-roll). Use config for sheet ID and tab names (content_log_tab, revenue_tracker_tab). Read Content Log and Revenue Tracker ranges; normalize header keys (e.g. strtolower) when parsing rows to avoid Content Log "Date" vs "date" breakage (RESEARCH pitfall).
    Expose GET /api/dashboard/revenue (or equivalent) that returns aggregates for the Revenue dashboard: post counts (from Content Log), traffic estimates (from Sheets or derived), affiliate/revenue data (from Revenue Tracker). Response shape must match what ui/revenue-dashboard.tsx expects: e.g. revenueData (monthly), trafficData, contentStats, agentActivity, topPosts (or map existing hardcoded keys). Cache response with Cache::remember(300) (5 min). Do not require a new "dashboard" sheet — derive from existing tabs only (CONTEXT).
    Register route under auth if existing routes use auth; otherwise document for owner to add middleware. Implement RevenueDashboardApiTest to assert 200 and presence of at least one expected key (e.g. content_stats or revenue_data).
  </action>
  <verify>
    <automated>cd laravel && php artisan test --filter=RevenueDashboard</automated>
  </verify>
  <done>GET /api/dashboard/revenue returns JSON with live-derived aggregates; test green.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Revenue dashboard UI to API</name>
  <files>ui/revenue-dashboard.tsx</files>
  <action>
    Replace hardcoded revenueData, trafficData, contentStats, agentActivity, topPosts in ui/revenue-dashboard.tsx with data fetched from the Laravel API (e.g. GET /api/dashboard/revenue). Use useEffect + fetch (or axios) with a configurable base URL (env or prop, e.g. process.env.REACT_APP_API_URL or window.API_BASE). Handle loading and error state; show fallback or message if fetch fails. Preserve existing chart and tab structure; only the data source changes from constants to API response. If the dashboard is embedded in Laravel Blade, pass API base URL from the view (e.g. data attribute or global).
  </action>
  <verify>
    <automated>grep -E "fetch|useEffect|/api/dashboard/revenue" ui/revenue-dashboard.tsx | head -5</automated>
  </verify>
  <done>Revenue dashboard component fetches from Laravel API; no hardcoded demo arrays for revenue/traffic/content/agents/topPosts.</done>
</task>

</tasks>

<verification>
- GET /api/dashboard/revenue returns 200 with aggregates from Sheets.
- Revenue dashboard UI shows data from API (manual check: open dashboard, confirm no static sample data).
- php artisan test --filter=RevenueDashboard passes.
</verification>

<success_criteria>
- DASH-01 satisfied: Revenue dashboard fetches live data from Google Sheets via Laravel API; owner sees real post counts, traffic estimates, and affiliate data.
</success_criteria>

<output>
After completion, create .planning/phases/05-live-dashboards-monitoring/05-01-SUMMARY.md
</output>
