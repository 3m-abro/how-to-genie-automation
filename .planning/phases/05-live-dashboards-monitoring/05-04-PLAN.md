---
phase: 05-live-dashboards-monitoring
plan: "04"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - laravel/app/Console/Commands/WeeklySummaryCommand.php
  - laravel/app/Mail/WeeklySummaryMailable.php
  - laravel/app/Console/Kernel.php
  - laravel/config/services.php
autonomous: true
requirements:
  - DASH-04
user_setup:
  - service: smtp
    why: "Weekly summary email delivery"
    env_vars:
      - name: MAIL_* (MAIL_MAILER, MAIL_HOST, etc.)
        source: "Laravel .env mail config"
      - name: WEEKLY_SUMMARY_RECIPIENT or MAIL_FROM
        source: "Owner email for weekly summary"
must_haves:
  truths:
    - "Weekly summary email is sent (Sunday or config day) with posts published, top performer, revenue estimate"
  artifacts:
    - path: laravel/app/Console/Commands/WeeklySummaryCommand.php
      provides: "Read Sheets, build summary, send email"
    - path: laravel/app/Mail/WeeklySummaryMailable.php
      provides: "Email content (posts, top performer, revenue)"
  key_links:
    - from: WeeklySummaryCommand
      to: "GoogleSheetsService"
      via: "Content Log + Revenue Tracker reads"
      pattern: "content_log|revenue_tracker|readRange"
    - from: WeeklySummaryCommand
      to: "Mail"
      via: "Mail::to()->send(WeeklySummaryMailable)"
      pattern: "WeeklySummaryMailable|Mail::to"
---

<objective>
Implement the weekly summary: a scheduled command that reads Content Log and Revenue Tracker from Google Sheets, builds a summary (posts published, top performer, revenue estimate), and sends one email to the owner (DASH-04). Sunday as baseline; day configurable via config.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-live-dashboards-monitoring/05-CONTEXT.md
@.planning/phases/05-live-dashboards-monitoring/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WeeklySummaryCommand and WeeklySummaryMailable</name>
  <files>laravel/app/Console/Commands/WeeklySummaryCommand.php, laravel/app/Mail/WeeklySummaryMailable.php</files>
  <action>
    Create WeeklySummaryCommand (signature e.g. weekly:summary). In handle(): resolve GoogleSheetsService (from Plan 01), read Content Log and Revenue Tracker tabs for the current week (filter by date or use last 7 days). Compute: posts_published (count from Content Log), top_performer (e.g. post with highest views or revenue from Sheets; column names may vary — normalize per RESEARCH), revenue_estimate (sum from Revenue Tracker or derived). Optional: add streak/health line per CONTEXT. Build an array or DTO with these fields.
    Create WeeklySummaryMailable that accepts the summary data and renders a simple email (Blade view or inline text): list posts published, top performer title/link, revenue estimate, optional streak. Use Mail::to(config('mail.weekly_summary_recipient') or config('services.weekly_summary.recipient'))->send(new WeeklySummaryMailable($summary)). Do not cache the summary (fresh read each run). Implement or update WeeklySummaryCommandTest to assert command runs and sends mail (mock Sheets and Mail::fake()).
  </action>
  <verify>
    <automated>cd laravel && php artisan test --filter=WeeklySummary</automated>
  </verify>
  <done>Weekly summary command builds summary from Sheets and sends one email; test green with mocked Sheets and Mail.</done>
</task>

<task type="auto">
  <name>Task 2: Schedule weekly summary (Sunday or config)</name>
  <files>laravel/app/Console/Kernel.php, laravel/config/services.php</files>
  <action>
    Register WeeklySummaryCommand in the scheduler to run weekly: e.g. $schedule->command('weekly:summary')->weeklyOn(1, '08:00') for Sunday 8 AM (1 = Sunday in Laravel). Use config('services.weekly_summary.day', 1) and config('services.weekly_summary.time', '08:00') so the day and time are configurable (CONTEXT). Add config keys to config/services.php if not present. Document WEEKLY_SUMMARY_RECIPIENT or equivalent in .env.example.
  </action>
  <verify>
    <automated>grep -E "weekly:summary|weeklyOn|WeeklySummary" laravel/app/Console/Kernel.php</automated>
  </verify>
  <done>Weekly summary runs every Sunday (or configured day) and sends email to owner.</done>
</task>

</tasks>

<verification>
- php artisan weekly:summary runs without error (with mocked Sheets and Mail::fake()).
- Scheduler entry present for weekly:summary with configurable day/time.
- php artisan test --filter=WeeklySummary passes.
</verification>

<success_criteria>
- DASH-04 satisfied: Weekly summary report is auto-generated and sent to owner (posts published, top performer, revenue estimate).
</success_criteria>

<output>
After completion, create .planning/phases/05-live-dashboards-monitoring/05-04-SUMMARY.md
</output>
