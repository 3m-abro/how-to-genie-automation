---
phase: 02-distribution-growth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - growth/HowTo-Genie v4.0 — Multi-Language Expansion Engine.json
  - .planning/phases/02-distribution-growth/02-CONFIG-KEYS.md
autonomous: true
requirements:
  - GROW-01
user_setup: []
must_haves:
  truths:
    - "Multi-language workflow runs at 2 PM when MULTI_LANGUAGE_ENABLED is true"
    - "When disabled or no valid post today, workflow exits without writing to Multilingual Content"
    - "When today's post exists (status ≠ publish_failed), translations publish to 8 language subdomains and 8 rows appear in Multilingual Content with language codes and WP URLs"
  artifacts:
    - path: growth/HowTo-Genie v4.0 — Multi-Language Expansion Engine.json
      provides: "Config-gated multi-language translate and publish to 8 subdomains"
      contains: "Execute Workflow (Config Loader), IF MULTI_LANGUAGE_ENABLED, Get today's post filter, 8 publish nodes"
    - path: .planning/phases/02-distribution-growth/02-CONFIG-KEYS.md
      provides: "Phase 2 config keys and tab names for htg_config and Sheets"
  key_links:
    - from: "Schedule Trigger"
      to: "Execute Workflow (Config Loader)"
      via: "first node after trigger"
      pattern: "executeWorkflow.*Config Loader"
    - from: "Get today's post Code node"
      to: "Content Log"
      via: "filter date === today (config TZ), status !== publish_failed"
      pattern: "noPostToday|valid.*row"
    - from: "Fetch English Article / Publish nodes"
      to: "config"
      via: "WORDPRESS_URL, sheet ID, Multilingual Content tab from $('⚙️ Load Config').item.json"
---

<objective>
Activate the Multi-Language Expansion workflow so it runs at 2 PM only when enabled in config, uses today's published post from Content Log (Phase 1 shape), and publishes translations to 8 language subdomains with rows in the Multilingual Content sheet.

Purpose: GROW-01 — today's post reaches audiences in 9 languages (en + 8 translations) without manual promotion; enable/disable via htg_config only.
Output: Modified workflow JSON with Config Loader at start, enable gate, today+status filter, config-driven sheet/URLs; 02-CONFIG-KEYS.md with all Phase 2 keys.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-distribution-growth/02-CONTEXT.md
@.planning/phases/02-distribution-growth/02-RESEARCH.md
@.planning/phases/02-distribution-growth/02-VALIDATION.md

Reference: core/08_Orchestrator_v3.json — first node after trigger is "⚙️ Load Config" (executeWorkflow); downstream nodes use `$('⚙️ Load Config').item.json.KEY`. Config Loader sub-workflow: core/01_Config_Loader.json (output = single item with Key/Value merged into one object).
Content Log shape (Phase 1): date, status, wp_url (or equivalent); slug = last path segment of wp_url.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config Loader, enable gate, and Get today's post</name>
  <files>growth/HowTo-Genie v4.0 — Multi-Language Expansion Engine.json</files>
  <action>
    Insert Execute Workflow as the first node after "🌍 Translation Pipeline Trigger (2PM Daily)". Configure it to run the Config Loader sub-workflow (core/01_Config_Loader.json); name the node "⚙️ Load Config". Use default "wait for sub-workflow" so output is available. Add a Code node that reads `$('⚙️ Load Config').item.json` and normalizes MULTI_LANGUAGE_ENABLED (true or string "true" → true). Add an IF node: condition MULTI_LANGUAGE_ENABLED === true; false branch must stop execution (no downstream nodes, no log). Replace "📄 Get Today's English Post" with a Google Sheets node that reads from documentId = `$('⚙️ Load Config').item.json.GOOGLE_SHEET_ID || SPREADSHEET_ID` and sheetName = `$('⚙️ Load Config').item.json.CONTENT_LOG_TAB || 'Content Log'`. Add a Code node "Filter today's post" that: gets timezone from config CONTENT_DAY_TIMEZONE or TIMEZONE or 'UTC'; computes today as YYYY-MM-DD with toLocaleDateString('en-CA', { timeZone }); filters input rows where date (first 10 chars) === today and status (lowercased) !== 'publish_failed'; if no rows, return one item `{ json: { noPostToday: true } }` and ensure no path leads to translation or Multilingual Content append; if rows exist, return one item with the latest row plus slug derived from wp_url (last path segment). Wire the true branch of the IF (enabled) to the new Sheets read; wire Filter output so noPostToday stops the workflow (no log per CONTEXT).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('growth/HowTo-Genie v4.0 — Multi-Language Expansion Engine.json'))"</automated>
  </verify>
  <done>Workflow has ⚙️ Load Config → Code (normalize) → IF enabled → Sheets (config) → Code (Filter today's post); when disabled or no post, no Multilingual Content write.</done>
</task>

<task type="auto">
  <name>Task 2: Config-driven fetch, publish URLs, Multilingual Content log, and config doc</name>
  <files>growth/HowTo-Genie v4.0 — Multi-Language Expansion Engine.json, .planning/phases/02-distribution-growth/02-CONFIG-KEYS.md</files>
  <action>
    Replace "📥 Fetch English Article" URL: use `$('⚙️ Load Config').item.json.WORDPRESS_URL` (trim trailing slash) + '/wp-json/wp/v2/posts?slug=' + slug from Filter today's post output. Ensure only the path where noPostToday is false continues to Fetch and downstream. Wire all 8 "Publish to X Site" nodes to use subdomain base URL from config (e.g. https://{subdomain}.howtogenie.com or config-driven; CONTEXT specifies es., pt., de., fr., hi., id., ar., ja. subdomains). Use WORDPRESS_URL pattern: if config has per-language URL keys use those; otherwise derive from main URL (e.g. replace host with subdomain). Ensure "📊 Log Translated Posts" (or equivalent) appends to Google Sheets using sheet ID and tab from config; add config key MULTILINGUAL_CONTENT_TAB (or MULTILINGUAL_CONTENT_TAB) with fallback "Multilingual Content". Create .planning/phases/02-distribution-growth/02-CONFIG-KEYS.md listing all Phase 2 config keys: MULTI_LANGUAGE_ENABLED, MESSAGING_DIGEST_ENABLED, MESSAGING_SUBSCRIBERS_TAB, CONTENT_DAY_TIMEZONE (optional), MULTILINGUAL_CONTENT_TAB (or same), CONTENT_LOG_TAB, GOOGLE_SHEET_ID/SPREADSHEET_ID, WORDPRESS_URL; and required Sheets tabs (Multilingual Content, Messaging Subscribers, Messaging Distribution Log) so user can add keys to htg_config and create tabs.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('growth/HowTo-Genie v4.0 — Multi-Language Expansion Engine.json'))"</automated>
  </verify>
  <done>Fetch uses config WORDPRESS_URL + slug; 8 publish nodes use config/subdomain URLs; Multilingual Content log uses config sheet ID and tab; 02-CONFIG-KEYS.md exists with Phase 2 keys and tab names.</done>
</task>

</tasks>

<verification>
- Run workflow with MULTI_LANGUAGE_ENABLED=false or missing: no Sheets append, no HTTP calls (manual).
- Run with no valid Content Log row for today: workflow stops without Multilingual Content row (manual).
- Run with valid today row and enabled: 8 WP publishes and 8 Multilingual Content rows with language codes and WP URLs (manual).
</verification>

<success_criteria>
- Multi-language workflow runs at 2 PM when enabled; when disabled or no post, exits without writing to Multilingual Content.
- Translated posts appear in Multilingual Content tab with language codes and WP URLs.
- All sheet IDs, tabs, and WORDPRESS_URL come from Config Loader; toggling MULTI_LANGUAGE_ENABLED turns workflow on/off without re-importing JSON.
</success_criteria>

<output>
After completion, create .planning/phases/02-distribution-growth/02-01-SUMMARY.md
</output>
