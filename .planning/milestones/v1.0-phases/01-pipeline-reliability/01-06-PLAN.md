---
phase: 01-pipeline-reliability
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - core/08_Orchestrator_v3.json
  - htg_config.csv
autonomous: true
requirements:
  - PIPE-05
user_setup: []

must_haves:
  truths:
    - "Changing a value in htg_config.csv (including Content Log sheet ID and tab name) takes effect on the next scheduled run without re-importing the workflow JSON"
  artifacts:
    - path: core/08_Orchestrator_v3.json
      provides: "📊 Log to Google Sheets node uses runtime config for documentId and sheetName"
      pattern: "Load Config.*GOOGLE_SHEET_ID|SPREADSHEET_ID|CONTENT_LOG_TAB"
    - path: htg_config.csv
      provides: "CONTENT_LOG_TAB key for Content Log tab name (optional runtime override)"
      contains: "CONTENT_LOG_TAB"
  key_links:
    - from: "core/08_Orchestrator_v3.json (📊 Log to Google Sheets)"
      to: "⚙️ Load Config"
      via: "documentId and sheetName expressions"
      pattern: "\$\\('⚙️ Load Config'\\)\\.item\\.json"
---

<objective>
Close PIPE-05 verification gap: make the Content Log write use runtime config so that changing GOOGLE_SHEET_ID or Content Log tab name in htg_config takes effect on the next run without re-importing the workflow JSON.

Gap source: 01-VERIFICATION.md — "📊 Log to Google Sheets" uses $vars.GOOGLE_SHEET_ID and hardcoded sheetName "Content Log" instead of Load Config output.
</objective>

<execution_context>
@/home/maqsood/.codex/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-reliability/01-VERIFICATION.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Log to Google Sheets to Load Config for documentId and sheetName</name>
  <files>core/08_Orchestrator_v3.json</files>
  <action>
    In core/08_Orchestrator_v3.json, locate the node named "📊 Log to Google Sheets" (type n8n-nodes-base.googleSheets). Update its parameters:
    (1) documentId.value: change from "{{ $vars.GOOGLE_SHEET_ID }}" to "={{ $('⚙️ Load Config').item.json.GOOGLE_SHEET_ID || $('⚙️ Load Config').item.json.SPREADSHEET_ID }}"
    (2) sheetName.value: change from "Content Log" to "={{ $('⚙️ Load Config').item.json.CONTENT_LOG_TAB || 'Content Log' }}"
    Match the expression style used elsewhere in this file (e.g. "📊 Write to Rejected Posts Sheet" uses the same Load Config pattern). Do not change operation, columns, or options.
  </action>
  <verify>
    <automated>grep -q "Load Config').item.json.GOOGLE_SHEET_ID" core/08_Orchestrator_v3.json && grep -q "CONTENT_LOG_TAB" core/08_Orchestrator_v3.json</automated>
  </verify>
  <done>📊 Log to Google Sheets reads documentId and sheetName from Load Config; changing config takes effect on next run without re-import.</done>
</task>

<task type="auto">
  <name>Task 2: Add CONTENT_LOG_TAB to htg_config.csv</name>
  <files>htg_config.csv</files>
  <action>
    Add a row to htg_config.csv: key CONTENT_LOG_TAB, value Content Log. Use the same key,value format as existing rows (e.g. REJECTED_POSTS_TAB,Rejected Posts). Place it near other sheet tab keys (e.g. after BLOG_IDEA_TAB or near REJECTED_POSTS_TAB). This documents the config key and allows runtime override of the Content Log tab name without re-importing the workflow.
  </action>
  <verify>
    <automated>grep -q "CONTENT_LOG_TAB,Content Log" htg_config.csv</automated>
  </verify>
  <done>htg_config.csv contains CONTENT_LOG_TAB so Content Log tab name is configurable at runtime.</done>
</task>

</tasks>

<verification>
- Re-run phase verification after execution: truth 5 (config changes take effect) should be fully satisfied; PIPE-05 status should move from partial to satisfied.
- Manual check: Change GOOGLE_SHEET_ID or CONTENT_LOG_TAB in n8n htg_config data table (or CSV if synced); run orchestrator; Content Log write should use the new values.
</verification>

<success_criteria>
- "📊 Log to Google Sheets" node has documentId and sheetName sourced from $('⚙️ Load Config').item.json (with fallbacks).
- htg_config.csv includes CONTENT_LOG_TAB with value "Content Log".
- Requirement PIPE-05 is fully addressed for Content Log destination.
</success_criteria>

<output>
After completion, create .planning/phases/01-pipeline-reliability/01-06-SUMMARY.md
</output>
