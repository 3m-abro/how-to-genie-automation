---
phase: 01-pipeline-reliability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core/08_Orchestrator_v3.json
autonomous: true
requirements:
  - PIPE-02
user_setup: []

must_haves:
  truths:
    - "Agents 1–5 call Ollama Agent (Central) sub-workflow with temperature per agent"
    - "QC output is parsed to a boolean; QC Approved? branches on parsed approved, not string-contains"
    - "Parse & Validate QC exists and sets parse_error when JSON parse fails"
  artifacts:
    - path: core/08_Orchestrator_v3.json
      provides: "executeWorkflow nodes for Agents 1–5, Parse & Validate QC Code node, QC Approved? IF on $json.approved"
  key_links:
    - from: "Agent 1–5 nodes"
      to: "core/Ollama Agent (Central).json"
      via: "executeWorkflow with workflowInputs (model, user_message, system_message, temperature, num_predict)"
      pattern: "executeWorkflow|workflowInputs"
    - from: "✅ QC Approved?"
      to: "✅ Parse & Validate QC"
      via: "IF condition on parsed boolean"
      pattern: "\\.approved"
---

<objective>
Satisfy PIPE-02: migrate Agents 1–5 from httpRequest (direct Ollama) to executeWorkflow calling Ollama Agent (Central); add Parse & Validate QC Code node and make QC Approved? branch on parsed boolean.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-reliability/01-CONTEXT.md
@.planning/phases/01-pipeline-reliability/01-RESEARCH.md
</context>

<interfaces>
Ollama Agent (Central) expects workflowInputs: user_message (string), system_message (string), model (string), temperature (number), num_predict (number). Output: Normalize Output node yields json.message.content (string). Use parameter name "model" not "ollama_model" (RESEARCH.md).
Per-agent temperatures: Agent 1 = 0.7, 2 = 0.8, 3 = 0.9, 4 = 0.4, 5 = 0.3.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace Agents 1–5 httpRequest with executeWorkflow to Ollama Central</name>
  <files>core/08_Orchestrator_v3.json</files>
  <action>
    For each of the five nodes: "🔍 Agent 1: Research & Topic", "✍️ Agent 2: Content Writer", "🫂 Agent 3: Humanizer", "🎯 Agent 4: SEO & Monetization", "🛡️ Agent 5: Quality Control" — replace the existing httpRequest node with an executeWorkflow node that calls the Ollama Agent (Central) sub-workflow. Preserve node names and positions. Set parameters.workflowId to the Central workflow (use id from core/Ollama Agent (Central).json or mode "list" with cachedResultName "Ollama Agent (Central)" / "HowTo-Genie — Ollama Agent (Central)"). Set workflowInputs.mappingMode "defineBelow" and workflowInputs.value: model = config OLLAMA_MODEL_SMART or "llama3.2:latest", user_message = the same prompt text currently in the httpRequest body (extract from existing jsonBody or prompt field), system_message = config PERSONA or default, temperature = per-agent value (0.7, 0.8, 0.9, 0.4, 0.3), num_predict = 4096. Set options.waitForSubWorkflow = true so the next node receives the LLM output. Fix Agent 0 if it passes ollama_model: use "model" in workflowInputs so Central receives the model. Do not add new credentials; reference existing Ollama credential in Central.
  </action>
  <verify>
    <automated>grep -c "executeWorkflow" core/08_Orchestrator_v3.json | xargs -I{} test {} -ge 6 && grep -q "waitForSubWorkflow" core/08_Orchestrator_v3.json</automated>
  </verify>
  <done>All five agent nodes are executeWorkflow calling Central with correct temperatures; Agent 0 uses "model" if it was wrong.</done>
</task>

<task type="auto">
  <name>Task 2: Add Parse & Validate QC node and fix QC Approved? condition</name>
  <files>core/08_Orchestrator_v3.json</files>
  <action>
    (1) Insert a new Code node "✅ Parse & Validate QC" immediately after "🛡️ Agent 5: Quality Control". Code: read $input.first().json (from Normalize Output pattern, so message.content or equivalent); extract JSON with raw.match(/```json\n([\s\S]*?)\n```/) || raw.match(/(\{[\s\S]*\})/); parse; if typeof qc.approved !== 'boolean' throw; return [{ json: qc }]. In catch, return fallback object with approved: false, average_score: 0, rejection_reason: 'QC parse failure', scores: {}, parse_error: true. (2) Reconnect: Agent 5 → Parse & Validate QC → QC Approved?. (3) Change the "✅ QC Approved?" IF node condition from string-contains on message.content to a boolean check on the parsed output: use expression that reads from the Parse & Validate QC node output, e.g. $json.approved === true or $('✅ Parse & Validate QC').item.json.approved. Remove any condition that checks for '"approved": true' in raw text.
  </action>
  <verify>
    <automated>grep -q "Parse & Validate QC" core/08_Orchestrator_v3.json && grep -q "parse_error" core/08_Orchestrator_v3.json</automated>
  </verify>
  <done>Parse & Validate QC exists after Agent 5; QC Approved? branches on parsed .approved boolean.</done>
</task>

</tasks>

<verification>
- No httpRequest nodes left for Agents 1–5 (grep for type httpRequest and node names).
- QC Approved? uses parsed boolean; no string-contains on "approved".
</verification>

<success_criteria>
- PIPE-02 prerequisite: All LLM agents go through Central; QC path uses parsed JSON.
</success_criteria>

<output>
After completion, create .planning/phases/01-pipeline-reliability/01-02-SUMMARY.md
</output>
