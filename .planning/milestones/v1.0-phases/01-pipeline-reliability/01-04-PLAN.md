---
phase: 01-pipeline-reliability
plan: 04
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - core/08_Orchestrator_v3.json
autonomous: true
requirements:
  - PIPE-04
user_setup:
  - task: "Create 'Rejected Posts' tab in Google Sheets with columns: date, topic, primary_keyword, qc_score, rejection_reasons, word_count, agent_fallbacks_used"
    why: "Required for QC rejection logging; cannot be created by workflow"
  - task: "Add REJECTED_POSTS_TAB to n8n htg_config data table with value 'Rejected Posts'"
    why: "Orchestrator reads tab name from config at runtime"
must_haves:
  truths:
    - "QC rejection writes a full row to Rejected Posts sheet"
    - "When topic came from backlog (_row), Blog Idea Backlog row is updated to status=rejected"
    - "QC rejection sends a Telegram alert with topic, score, reason"
  artifacts:
    - path: core/08_Orchestrator_v3.json
      provides: "Build QC Rejection Row Code node, Write to Rejected Posts Sheet node, Update Backlog node (conditional), Telegram QC Rejection node"
  key_links:
    - from: "✅ QC Approved? [false]"
      to: "📋 Build QC Rejection Row"
      via: "direct connection"
    - from: "📋 Build QC Rejection Row"
      to: "📊 Write to Rejected Posts Sheet"
      via: "append row with assembled fields"
    - from: "📋 Build QC Rejection Row"
      to: "Blog Idea Backlog"
      via: "Google Sheets update when _row present"
---

<objective>
Satisfy PIPE-04: replace the QC rejected dead-end (Alert Handler with empty inputs) with Build QC Rejection Row → Write to Rejected Posts Sheet, conditional Update Backlog to status=rejected, and Telegram QC rejection alert.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pipeline-reliability/01-CONTEXT.md
@.planning/phases/01-pipeline-reliability/01-RESEARCH.md
</context>

<interfaces>
Parse & Validate QC output: approved, average_score, rejection_reason, scores (object), parse_error. Parse & Validate Topic: topic, primary_keyword, _row (when from backlog), status. Assemble Final Article: word_count. Config: REJECTED_POSTS_TAB, GOOGLE_SHEET_ID. Backlog update: use Sheets update with matchingColumns Row_Number; only when topic._row exists and use_approved_topic true (Inject Approved Topic).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Build QC Rejection Row and Write to Rejected Posts Sheet</name>
  <files>core/08_Orchestrator_v3.json</files>
  <action>
    (1) Remove or bypass the existing "⚠️ Alert: QC Rejected" executeWorkflow call from the QC Approved? false branch. (2) Add Code node "📋 Build QC Rejection Row" that reads from: ✅ Parse & Validate QC, ✅ Parse & Validate Topic, 🔧 Assemble Final Article, ⚙️ Load Config. Build parse_error_agents list from Parse Prompt Package (agent_0), Topic (agent_1 fallback_topic/parse_error), Assemble (agent_4), Parse & Validate QC (agent_5). Output one item: { date (YYYY-MM-DD), topic, primary_keyword, qc_score, rejection_reasons (JSON.stringify of qc.scores or rejection_reason), word_count, agent_fallbacks_used (JSON.stringify of array), run_timestamp }. (3) Add Google Sheets node "📊 Write to Rejected Posts Sheet": operation append, documentId from config GOOGLE_SHEET_ID, sheetName from config REJECTED_POSTS_TAB. Map the fields from the Code node output. Connect: QC Approved? [false] → Build QC Rejection Row → Write to Rejected Posts Sheet.
  </action>
  <verify>
    <automated>grep -q "Build QC Rejection Row" core/08_Orchestrator_v3.json && grep -q "Write to Rejected Posts Sheet\|Rejected Posts" core/08_Orchestrator_v3.json</automated>
  </verify>
  <done>QC false branch writes a structured row to Rejected Posts sheet.</done>
</task>

<task type="auto">
  <name>Task 2: Conditional Backlog update and Telegram QC rejection alert</name>
  <files>core/08_Orchestrator_v3.json</files>
  <action>
    (1) Add Code node "📋 Build Backlog Rejection Update" after Build QC Rejection Row (parallel or same branch). It reads Parse & Validate Topic and Inject Approved Topic. If !use_approved_topic || !topic._row, return [{ json: { skip_backlog_update: true } }]. Else return [{ json: { Row_Number: topic._row, status: 'rejected', Status: 'rejected' } }]. (2) Add IF "Backlog Update Needed?" checking !$json.skip_backlog_update. True → Google Sheets update node: operation update, documentId GOOGLE_SHEET_ID, sheetName BLOG_IDEA_TAB, matchingColumns ['Row_Number'], columns to update Row_Number + status. False → no op. (3) Add Telegram node "📱 Telegram: QC Rejected": send message with text like "QC rejected: [topic] — score [X]/10, reasons: [reasons]. Fresh topic tomorrow." Use config or credentials for Telegram. Connect Build QC Rejection Row to Backlog builder and to Telegram so both run (or Backlog branch and Telegram from same parent). Ensure Telegram receives topic, qc_score, rejection_reasons from the rejection row.
  </action>
  <verify>
    <automated>grep -q "Backlog Rejection\|Telegram.*QC Rejected\|QC Rejected" core/08_Orchestrator_v3.json</automated>
  </verify>
  <done>Backlog updated to rejected when topic has _row; Telegram alert sent on every QC rejection.</done>
</task>

</tasks>

<verification>
- QC rejected path no longer calls Alert Handler with empty workflowInputs.
- Rejected Posts tab name read from REJECTED_POSTS_TAB.
</verification>

<success_criteria>
- PIPE-04: QC rejection writes to Rejected Posts sheet, updates Backlog when applicable, sends Telegram alert.
</success_criteria>

<output>
After completion, create .planning/phases/01-pipeline-reliability/01-04-SUMMARY.md
</output>
