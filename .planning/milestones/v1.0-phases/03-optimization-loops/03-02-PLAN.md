---
phase: 03-optimization-loops
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - growth/HowTo-Genie v4.0 — Viral Content Amplifier Engine.json
  - growth/HowTo-Genie v4.0 — Viral Amplifier Queue.json
  - .planning/phases/03-optimization-loops/03-CONFIG-KEYS.md
autonomous: true
requirements:
  - GROW-04
user_setup: []
must_haves:
  truths:
    - "Viral workflow runs every 6 hours when VIRAL_AMPLIFIER_ENABLED is true and appends qualifying rows to Viral Amplifier tab with promotion_status=pending"
    - "When disabled or no rows above threshold, workflow exits without writing"
    - "A separate Viral Amplifier Queue workflow reads pending rows, appends to Social Queue for re-promotion, and updates Viral Amplifier row to promotion_status=sent"
  artifacts:
    - path: growth/HowTo-Genie v4.0 — Viral Content Amplifier Engine.json
      provides: "Config-gated GA4 viral detection and append to Viral Amplifier tab"
      contains: "Execute Workflow (Config Loader), IF VIRAL_AMPLIFIER_ENABLED, GA4 runReport, Code viral threshold, Append to Viral Amplifier tab"
    - path: growth/HowTo-Genie v4.0 — Viral Amplifier Queue.json
      provides: "Read pending viral rows, queue to social, mark sent"
      contains: "Trigger, Config Loader, Read Viral Amplifier tab, Filter pending, append to Social Queue, update row to sent"
  key_links:
    - from: "Schedule Trigger (every 6h)"
      to: "Execute Workflow (Config Loader)"
      via: "first node after trigger"
      pattern: "executeWorkflow|Load Config"
    - from: "Detect Viral Code"
      to: "GA4 response"
      via: "VIRAL_VIEWS_7D_MIN, VIRAL_ENGAGEMENT_MIN from config; views_7d and engagement_rate from metricValues order"
      pattern: "views_7d|engagement_rate|promotion_status.*pending"
    - from: "Viral Amplifier Queue workflow"
      to: "Viral Amplifier tab"
      via: "read promotion_status=pending; update same row to sent"
      pattern: "pending|sent"
---

<objective>
Activate the Viral Content Amplifier so it runs every 6 hours when enabled, reads GA4 data only (no Search Console), applies config-driven viral thresholds, appends qualifying rows to a dedicated Viral Amplifier tab with promotion_status=pending and full metrics; then wire a new Viral Amplifier Queue workflow that reads pending rows, appends to the existing Social Queue for organic re-promotion, and updates the Viral Amplifier row to promotion_status=sent. No paid campaigns (Facebook/Google Ads/Reddit) in Phase 3.

Purpose: GROW-04 — system identifies high-performing posts from GA4 and triggers organic re-promotion via existing social queue.
Output: Modified Viral workflow JSON (Config Loader, gate, GA4-only, config thresholds, append to Viral tab); new Viral Amplifier Queue workflow JSON; 03-CONFIG-KEYS.md updated with Viral keys if not already from Plan 01.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-optimization-loops/03-CONTEXT.md
@.planning/phases/03-optimization-loops/03-RESEARCH.md
@.planning/phases/03-optimization-loops/03-VALIDATION.md

GA4 runReport: dimensions pagePath, pageTitle; metrics screenPageViews, averageSessionDuration, engagementRate, bounceRate in that order — map metricValues[0..3] accordingly. Viral condition: views_7d >= VIRAL_VIEWS_7D_MIN (default 5000) AND engagement_rate >= VIRAL_ENGAGEMENT_MIN (default 0.08). Viral Amplifier tab columns: date, post_url, post_title, viral_score, views_7d, engagement_rate, bounce_rate, avg_session_duration, amplify (true), promotion_status (pending|sent|skipped), detected_at. Per RESEARCH pitfall: when updating row to sent, use a stable identifier — e.g. append a row_id (detected_at + post_url) when appending; when reading pending, pass through row index or row_id; when updating, use Google Sheets update by row/range. Social Queue: same tab and column shape as used by social/11_Queue_Processor_v2.json (config SOCIAL_QUEUE_TAB); append rows with Status=Queued and Scheduled_Time so Queue Processor picks them up.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config Loader, enable gate, GA4-only, config thresholds, append to Viral Amplifier tab</name>
  <files>growth/HowTo-Genie v4.0 — Viral Content Amplifier Engine.json</files>
  <action>
    Insert Execute Workflow as the first node after "📊 Performance Monitor (Every 6 Hours)". Configure it to run Config Loader (core/01_Config_Loader.json); name the node "⚙️ Load Config". Add a Code node that normalizes VIRAL_AMPLIFIER_ENABLED (true or string "true" → true). Add an IF node: VIRAL_AMPLIFIER_ENABLED === true; false branch stops execution. Wire IF true to "📈 Fetch GA4 Performance Data". Remove or disconnect "🔍 Fetch Search Console Data" and any Merge that combines GSC with GA4 — Phase 3 is GA4-only. Set GA4 HTTP Request URL to use config: https://analyticsdata.googleapis.com/v1beta/properties/{{ $('⚙️ Load Config').item.json.GA4_PROPERTY_ID }}:runReport. Set Authorization header to Bearer {{ $('⚙️ Load Config').item.json.GOOGLE_ANALYTICS_TOKEN }}. Keep the existing runReport body (dateRanges 7daysAgo–today, dimensions pagePath/pageTitle, metrics screenPageViews, averageSessionDuration, engagementRate, bounceRate, orderBy screenPageViews desc, limit 50). Replace "🔥 Detect Viral Content" Code node so it: reads config from $('⚙️ Load Config').item.json; viewsMin = Number(config.VIRAL_VIEWS_7D_MIN) || 5000; engagementMin = Number(config.VIRAL_ENGAGEMENT_MIN) || 0.08; baseUrl = (config.WORDPRESS_URL || '').replace(/\/$/, '') or sensible default; iterates over GA4 rows only (no GSC); for each row: pagePath = dimensionValues[0].value, pageTitle = dimensionValues[1].value, views_7d = metricValues[0], avg_session_duration = metricValues[1], engagement_rate = metricValues[2], bounce_rate = metricValues[3]; if views_7d >= viewsMin && engagement_rate >= engagementMin && pagePath.length > 1, push object with date (today ISO date), post_url (baseUrl + pagePath), post_title, viral_score (e.g. round((views_7d/1000)*0.5 + engagement_rate*100*0.5)), views_7d, engagement_rate, bounce_rate, avg_session_duration, amplify: true, promotion_status: 'pending', detected_at: new Date().toISOString(), and row_id (e.g. detected_at + post_url for later update). Return one item per qualifying row (return out.map(o => ({ json: o }))). Remove or bypass "🚀 Viral Score ≥ 50?" IF and all nodes downstream of it that create paid campaigns (🎯 AI: Design Amplification Campaign, Parse Campaign Strategy, Create Facebook Ad Campaign, Create Google Ads Campaign, Queue Reddit Posts, Log Amplification Campaign). Replace with a single Google Sheets append node that writes to documentId and sheetName from config (VIRAL_AMPLIFIER_TAB from $('⚙️ Load Config').item.json; fallback "Viral Amplifier"). Map columns: date, post_url, post_title, viral_score, views_7d, engagement_rate, bounce_rate, avg_session_duration, amplify, promotion_status, detected_at, row_id (or equivalent). Ensure no hardcoded YOUR_GOOGLE_SHEET_ID or YOUR_GA4_PROPERTY_ID.
  </action>
  <verify>
    <automated>node -e "const w=require('fs').readFileSync('growth/HowTo-Genie v4.0 — Viral Content Amplifier Engine.json'); const j=JSON.parse(w); const names=j.nodes.map(x=>x.name); if(!names.some(x=>x&&(x.includes('Load Config')||x.includes('Config Loader')))) throw new Error('Missing Config Loader'); const gsc=names.some(x=>x&&x.includes('Search Console')); const ga4=names.some(x=>x&&x.includes('GA4')); if(!ga4) throw new Error('GA4 node required'); const hasAppend=j.nodes.some(n=>n.parameters&&n.parameters.operation==='append'&&n.parameters.sheetName&&(String(n.parameters.sheetName).includes('VIRAL')||String(n.parameters.sheetName).includes('Viral'))); if(!hasAppend&&!j.nodes.some(n=>n.parameters&&n.parameters.sheetName&&String(n.parameters.sheetName).includes('$'))) throw new Error('Viral Amplifier append must use config'); process.exit(0)"</automated>
  </verify>
  <done>Viral workflow has Load Config → IF enabled → GA4 (config URL/token) → Code (config thresholds, GA4-only) → Append to Viral Amplifier tab with promotion_status=pending; GSC and paid campaign nodes removed or bypassed.</done>
</task>

<task type="auto">
  <name>Task 2: Viral Amplifier Queue workflow — read pending, append to Social Queue, update to sent</name>
  <files>growth/HowTo-Genie v4.0 — Viral Amplifier Queue.json</files>
  <action>
    Create a new workflow JSON "HowTo-Genie v4.0 — Viral Amplifier Queue" with: (1) Schedule Trigger (e.g. "0 */6 * * *" every 6 hours or "0 12 * * *" daily — Claude's discretion) so it runs after viral rows can exist. (2) Execute Workflow node "⚙️ Load Config" running core/01_Config_Loader.json. (3) Google Sheets node "Read Viral Amplifier" — operation read (readRange or read), documentId from config GOOGLE_SHEET_ID, sheetName from config VIRAL_AMPLIFIER_TAB. (4) Code node "Filter pending" — filter input rows where promotion_status (or Promotion_Status) === 'pending'; attach _row_number or row_id to each item for later update; if none, return [{ json: { no_pending: true } }]. (5) For each pending row: either (a) use a Loop or SplitInBatches, or (b) process all in one Code that outputs one item per row. Each item must include post_url, post_title (or equivalent), and row identifier for update. (6) Append to Social Queue: Google Sheets append to documentId = config.GOOGLE_SHEET_ID, sheetName = config.SOCIAL_QUEUE_TAB (fallback "Social Queue"). Columns must match what Queue Processor expects: e.g. Post_URL, Post_Title or Title, Status = 'Queued', Scheduled_Time = now or in 1 hour (ISO or format Queue Processor uses). If the project uses Blotato to format before queueing, add Execute Workflow to run the Blotato sub-workflow with article_url and article_title from the viral row, then append Blotato output to Social Queue; otherwise append viral row fields directly to Social Queue in the expected column shape. (7) Update Viral Amplifier row to promotion_status=sent. Use Google Sheets update operation: same documentId and sheetName as Viral Amplifier tab; identify row by row number (e.g. from readRange, row index + 2 for 1-based header) or by a key column (row_id) if the sheet supports update by value. If n8n update requires range, compute range from _row_number (e.g. row 2 → A2:Z2 for promotion_status column). Set promotion_status (or Promotion_Status) column to 'sent'. Ensure credentials and config reference the same GOOGLE_SHEET_ID and tab names as the Viral Amplifier Engine. Save as growth/HowTo-Genie v4.0 — Viral Amplifier Queue.json with valid n8n workflow structure (nodes, connections, pinData if any, settings).
  </action>
  <verify>
    <automated>node -e "const w=require('fs').readFileSync('growth/HowTo-Genie v4.0 — Viral Amplifier Queue.json'); const j=JSON.parse(w); const names=j.nodes.map(x=>x.name); if(!j.name||!j.nodes.length) throw new Error('Invalid workflow'); if(!names.some(x=>x&&(x.includes('Config')||x.includes('Load Config')))) throw new Error('Missing Config'); if(!names.some(x=>x&&(x.includes('Viral')||x.includes('pending')||x.includes('Read')))) throw new Error('Missing read Viral Amplifier'); if(!names.some(x=>x&&(x.includes('Social')||x.includes('Queue')||x.includes('append')))) throw new Error('Missing append to Social Queue'); process.exit(0)"</automated>
  </verify>
  <done>Viral Amplifier Queue workflow exists; reads Viral Amplifier tab for promotion_status=pending; appends to Social Queue with correct columns; updates Viral Amplifier row to promotion_status=sent.</done>
</task>

</tasks>

<verification>
- Run Viral workflow with VIRAL_AMPLIFIER_ENABLED=false: no append (manual).
- Run Viral workflow with enabled and GA4 returning data above threshold: rows appear in Viral Amplifier tab with promotion_status=pending (manual).
- Run Viral Amplifier Queue with pending row(s): Social Queue gets new row(s), Viral Amplifier row(s) updated to sent (manual per 03-VALIDATION.md).
</verification>

<success_criteria>
- Viral workflow runs every 6 hours when enabled; appends qualifying GA4 rows to Viral Amplifier tab with full metrics and promotion_status=pending.
- Viral Amplifier Queue workflow picks up pending rows, adds them to Social Queue for organic re-promotion, and marks rows sent.
- No GSC or paid campaigns in Phase 3; all sheet/tab and flags from config.
</success_criteria>

<output>
After completion, create .planning/phases/03-optimization-loops/03-02-SUMMARY.md
</output>
