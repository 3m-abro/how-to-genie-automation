---
phase: 03-optimization-loops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - growth/HowTo-Genie v4.0 — A_B Testing & Optimization Engine.json
  - .planning/phases/03-optimization-loops/03-CONFIG-KEYS.md
autonomous: true
requirements:
  - GROW-03
user_setup: []
must_haves:
  truths:
    - "A/B workflow runs at 6 AM when A_B_TESTING_ENABLED is true"
    - "When disabled or no valid post yesterday (or status = publish_failed), workflow exits without writing to A/B tab"
    - "When yesterday's post exists, variants (headline + intro + CTA) are generated, parsed with success/data/error envelope, and one row is appended to the A/B tab with original + variant fields and winner column (manual/future)"
  artifacts:
    - path: growth/HowTo-Genie v4.0 — A_B Testing & Optimization Engine.json
      provides: "Config-gated A/B variant generation and log to Sheets only"
      contains: "Execute Workflow (Config Loader), IF A_B_TESTING_ENABLED, Read Content Log, yesterday filter Code, Fetch WP post, LLM Generate Test Variants, Parse & Validate, Append to A/B tab"
    - path: .planning/phases/03-optimization-loops/03-CONFIG-KEYS.md
      provides: "Phase 3 config keys (A/B and Viral) for htg_config and Sheets tabs"
  key_links:
    - from: "Schedule Trigger (6 AM)"
      to: "Execute Workflow (Config Loader)"
      via: "first node after trigger"
      pattern: "executeWorkflow|Load Config"
    - from: "Filter yesterday Code"
      to: "Content Log"
      via: "date === yesterday (config TZ), status !== publish_failed"
      pattern: "noPostYesterday|yesterday"
    - from: "Parse & Validate"
      to: "LLM output"
      via: "success/data/error envelope, fallback on parse error"
      pattern: "success.*data.*error"
---

<objective>
Activate the A/B Testing workflow so it runs at 6 AM only when enabled in config, uses yesterday's published post from Content Log, generates headline + intro + CTA variants via LLM, and logs them to a dedicated A/B tab in Google Sheets. No WordPress injection, no draft posts, no custom WP endpoints; winner column exists but is manual or future-auto.

Purpose: GROW-03 — system tests content variants and logs them for review; enable/disable via config.
Output: Modified A/B workflow JSON with Config Loader, enable gate, yesterday+status filter, config-driven sheet/tab, Parse & Validate with envelope; 03-CONFIG-KEYS.md with Phase 3 keys.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-optimization-loops/03-CONTEXT.md
@.planning/phases/03-optimization-loops/03-RESEARCH.md
@.planning/phases/03-optimization-loops/03-VALIDATION.md

Config Loader: same as Phase 2 — first node after trigger runs core/01_Config_Loader.json; downstream use $('⚙️ Load Config').item.json. Content Log shape: date, status, wp_url (or equivalent); slug = last path segment of wp_url. Yesterday = CONTENT_DAY_TIMEZONE (or TIMEZONE or 'UTC'), today in that TZ, then yesterday = today - 1 day ISO date. Per CONTEXT: variants in Sheets only; no WP injection; no 6 PM results flow.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config Loader, enable gate, Read Content Log, yesterday filter</name>
  <files>growth/HowTo-Genie v4.0 — A_B Testing & Optimization Engine.json</files>
  <action>
    Insert Execute Workflow as the first node after "🧪 A/B Test Setup (Daily 6AM)". Configure it to run the Config Loader sub-workflow (core/01_Config_Loader.json); name the node "⚙️ Load Config". Add a Code node that reads $('⚙️ Load Config').item.json and normalizes A_B_TESTING_ENABLED (true or string "true" → true). Add an IF node: condition A_B_TESTING_ENABLED === true; false branch must stop execution (no downstream nodes). Replace "📄 Get Yesterday's Post" with a Google Sheets node that reads from documentId = $('⚙️ Load Config').item.json.GOOGLE_SHEET_ID and sheetName = $('⚙️ Load Config').item.json.CONTENT_LOG_TAB (fallback "Content Log"). Add a Code node "Filter yesterday's post" that: gets timezone from config CONTENT_DAY_TIMEZONE or TIMEZONE or 'UTC'; computes yesterday as today in that TZ minus 1 day (YYYY-MM-DD); detects column keys from first row (dateKey, statusKey, urlKey — same pattern as Phase 2); filters rows where date (first 10 chars) === yesterday and status (lowercased) !== 'publish_failed'; if no rows, return one item { json: { noPostYesterday: true } }; if rows exist, return one item with the latest row plus slug (last path segment of wp_url) and wpUrl. Wire: IF true → Sheets read → Filter. Ensure when noPostYesterday is true no path leads to Fetch Post or LLM or Append (add IF or route so only noPostYesterday false continues to Fetch Post Content). Remove or disconnect the entire 6 PM branch (📈 Test Results Check, Load Active Tests, Fetch Test Results, AI Analyze Test Results, Parse Analysis, and any nodes that update A/B results) so Phase 3 has no WP custom endpoints or results flow.
  </action>
  <verify>
    <automated>node -e "const w=require('fs').readFileSync('growth/HowTo-Genie v4.0 — A_B Testing & Optimization Engine.json'); JSON.parse(w); const n=JSON.parse(w).nodes; const names=n.map(x=>x.name); if(!names.some(x=>x&&(x.includes('Load Config')||x.includes('Config Loader')))) throw new Error('Missing Config Loader'); if(!names.some(x=>x&&x.includes('yesterday')||x&&x.includes('Filter'))) throw new Error('Missing yesterday filter'); process.exit(0)"</automated>
  </verify>
  <done>Workflow has Load Config → Code (normalize) → IF enabled → Sheets (config) → Code (Filter yesterday); when disabled or no post yesterday, no A/B tab write; 6 PM branch removed or disconnected.</done>
</task>

<task type="auto">
  <name>Task 2: Fetch WP post, LLM variants, Parse & Validate, Append to A/B tab; remove Inject A/B node; config doc</name>
  <files>growth/HowTo-Genie v4.0 — A_B Testing & Optimization Engine.json, .planning/phases/03-optimization-loops/03-CONFIG-KEYS.md</files>
  <action>
    Ensure only the path where noPostYesterday is false goes to "📥 Fetch Post Content". Set Fetch Post Content URL to WordPress REST: use $('⚙️ Load Config').item.json.WORDPRESS_URL (trim trailing slash) + '/wp-json/wp/v2/posts?slug=' + slug from Filter output (or from $json.slug). Use the first post from the response if array. Wire Fetch output to "🎨 AI: Generate Test Variants". Ensure the LLM prompt (in the node or in a preceding prompt node) starts with "Return only valid JSON. No text. No markdown. No explanations." and includes a schema for success/data/error envelope and data containing: original_title, original_intro, original_cta, variant_title, variant_intro, variant_cta (one alternate per element per CONTEXT). Use headline + intro + CTA as variant types. Replace or update "📋 Parse Test Suite" so it: reads raw response from LLM; strips ```json if present; parses JSON; validates success (boolean); on success uses data for variant fields; on failure or parse error returns fallback object with success: false, data: null, error: { code, message }. Output one item with test_id (e.g. TEST_ + Date.now()), post_url, original_title, original_intro, original_cta, variant_title, variant_intro, variant_cta, created_at (ISO), status ('ready' or 'logged'), winner (empty string or 'TBD' — manual per CONTEXT). Remove the "💉 Inject A/B Test Code" node entirely and wire Parse output directly to the Sheets append node. Update "📊 Log Active Test" to: documentId and sheetName from config (AB_TESTS_TAB or AB_TESTS_TAB from $('⚙️ Load Config').item.json; fallback "AB Tests" or "AB Tests Active"). Map columns to: test_id, post_url, original_title, original_intro, original_cta, variant_title, variant_intro, variant_cta, created_at, status, winner (or equivalent header names). Do not add WordPress injection or custom endpoints. Create .planning/phases/03-optimization-loops/03-CONFIG-KEYS.md listing Phase 3 config keys: A_B_TESTING_ENABLED, AB_TESTS_TAB, CONTENT_LOG_TAB, GOOGLE_SHEET_ID, WORDPRESS_URL, CONTENT_DAY_TIMEZONE; and for Viral (Plan 02): VIRAL_AMPLIFIER_ENABLED, VIRAL_AMPLIFIER_TAB, VIRAL_VIEWS_7D_MIN, VIRAL_ENGAGEMENT_MIN, GA4_PROPERTY_ID, GOOGLE_ANALYTICS_TOKEN. Include required Sheets tabs (AB Tests or AB Tests Active, Viral Amplifier).
  </action>
  <verify>
    <automated>node -e "const w=require('fs').readFileSync('growth/HowTo-Genie v4.0 — A_B Testing & Optimization Engine.json'); const j=JSON.parse(w); const names=j.nodes.map(x=>x.name); if(names.some(x=>x&&x.includes('Inject A/B'))) throw new Error('Inject A/B node must be removed'); const logNode=j.nodes.find(n=>n.name&&n.name.includes('Log Active Test')||n.name&&n.name.includes('Log')); if(!logNode) throw new Error('Append to A/B tab node must exist'); require('fs').accessSync('.planning/phases/03-optimization-loops/03-CONFIG-KEYS.md'); process.exit(0)"</automated>
  </verify>
  <done>Fetch uses config WORDPRESS_URL + slug; LLM prompt JSON-only with schema; Parse uses success/data/error envelope; Inject A/B node removed; Append uses config AB_TESTS_TAB; 03-CONFIG-KEYS.md exists with Phase 3 keys and tab names.</done>
</task>

</tasks>

<verification>
- Run workflow with A_B_TESTING_ENABLED=false or missing: no Sheets append (manual per 03-VALIDATION.md).
- Run with no Content Log row for yesterday or status publish_failed: workflow stops without A/B append (manual).
- Run with valid yesterday row and enabled: one row in A/B tab with original + variant fields (manual).
</verification>

<success_criteria>
- A/B workflow runs at 6 AM when enabled; when disabled or no post yesterday, exits without writing to A/B tab.
- One variant (headline + intro + CTA) logged per run to dedicated A/B tab; owner can see variants in Sheets; winner column present for manual/future use.
- All sheet ID, tab, and feature flag from Config Loader; no WP injection or 6 PM results in Phase 3.
</success_criteria>

<output>
After completion, create .planning/phases/03-optimization-loops/03-01-SUMMARY.md
</output>
