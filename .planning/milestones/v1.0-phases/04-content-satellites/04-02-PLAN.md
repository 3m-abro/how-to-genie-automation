---
phase: 04-content-satellites
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - email/v3.0 — Email Newsletter Automation.json
autonomous: true
requirements:
  - GROW-06
user_setup: []
must_haves:
  truths:
    - "Webhook responds 200 OK when EMAIL_NEWSLETTER_ENABLED is false and does not add subscriber to ESP"
    - "When enabled, subscriber is added to ConvertKit or MailerLite according to EMAIL_PROVIDER config"
    - "First welcome email is sent by the ESP (sequence/automation), not by the workflow calling a 'send now' API"
    - "Subsequent sequence emails run on ESP-configured schedule without manual intervention"
  artifacts:
    - path: email/v3.0 — Email Newsletter Automation.json
      provides: Webhook, Config Loader, enable gate, single-provider branch, Add to ESP, Respond 200
  key_links:
    - from: Webhook workflow
      to: core/01_Config_Loader.json
      via: Execute Workflow at start (after webhook)
      pattern: Execute Workflow.*Config Loader
    - from: IF enabled false branch
      to: Respond to Webhook
      via: 200 OK response then stop
      pattern: 200|respondToWebhook
    - from: IF EMAIL_PROVIDER branch
      to: ConvertKit or MailerLite API
      via: Add to sequence/form or group (config-driven, one only)
      pattern: convertkit|mailerlite
---

<objective>
Refactor the email newsletter workflow so it is config-gated, single-provider (ConvertKit or MailerLite from EMAIL_PROVIDER), and relies on the ESP to send the first welcome email and the rest of a simplified sequence — no workflow-generated AI emails or "send now" API.

Purpose: New subscriber webhook adds the subscriber to the chosen ESP; ESP sends welcome within ~5 minutes and subsequent emails on schedule (GROW-06).
Output: Updated email/v3.0 — Email Newsletter Automation.json with Config Loader, EMAIL_NEWSLETTER_ENABLED gate (200 when disabled), EMAIL_PROVIDER branch, no YOUR_* in JSON, credentials via n8n.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-satellites/04-CONTEXT.md
@.planning/phases/04-content-satellites/04-RESEARCH.md

Existing: email/v3.0 — Email Newsletter Automation.json has Webhook → Validate & Tag → Valid Email? → parallel Add to ConvertKit and Add to MailerLite (with YOUR_* placeholders) → AI Welcome Email 1–3 and 4+5 → Build Email Sequence → Log Sequence to Sheets; Respond to Webhook. Config Loader must be added at start; enable gate must return 200 when disabled; single provider only; ESP sends first email (use sequence/form/group add, not workflow "send now").
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Config Loader and EMAIL_NEWSLETTER_ENABLED gate; respond 200 when disabled</name>
  <files>email/v3.0 — Email Newsletter Automation.json</files>
  <action>
In email/v3.0 — Email Newsletter Automation.json:
- Insert after "📬 Webhook: New Subscriber": Execute Workflow node "⚙️ Load Config" that calls core/01_Config_Loader (workflowId by ID; same pattern as Multi-Language/Video Engine). No input needed; wait for sub-workflow.
- Add Code node "🔧 Normalize Enable Flag": read $('⚙️ Load Config').item.json, set EMAIL_NEWSLETTER_ENABLED = raw === true || String(raw).toLowerCase() === 'true'; return { json: { EMAIL_NEWSLETTER_ENABLED: enabled, config: cfg } }.
- Add IF node "📧 Newsletter Enabled?": condition EMAIL_NEWSLETTER_ENABLED === true. False branch: connect to a "Respond to Webhook" node that sends HTTP 200 with body e.g. { success: true, message: "Thanks for signing up." } (or equivalent); then workflow ends (no further nodes). True branch: connect to existing "🔍 Validate & Tag Subscriber". So webhook trigger → Load Config → Normalize → IF enabled? → false: Respond 200, stop; true: Validate & Tag → (rest of flow).
- Ensure the webhook response mode is satisfied: when disabled, only one Respond to Webhook runs and exits. Do not add subscriber to ESP when disabled.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('email/v3.0 — Email Newsletter Automation.json')); const names=j.nodes.map(n=>n.name); if(!names.some(n=>/Load Config|Config Loader/i.test(n))) throw new Error('Missing Config Loader'); if(!names.some(n=>/Normalize|EMAIL_NEWSLETTER|enable/i.test(n))) throw new Error('Missing normalize enable'); if(!names.some(n=>/Newsletter Enabled|enabled/i.test(n))) throw new Error('Missing enable IF'); const r=names.filter(n=>/Respond|Webhook|response/i.test(n)); if(r.length===0) throw new Error('Missing response node'); console.log('OK');"</automated>
  </verify>
  <done>Config Loader is first after webhook; when EMAIL_NEWSLETTER_ENABLED is false, webhook responds 200 and workflow stops without adding to ESP.</done>
</task>

<task type="auto">
  <name>Task 2: Single-provider branch (EMAIL_PROVIDER), Add to ESP only, remove AI emails; credentials via n8n</name>
  <files>email/v3.0 — Email Newsletter Automation.json</files>
  <action>
In email/v3.0 — Email Newsletter Automation.json:
- After "✅ Valid Email?" (true branch), add IF or Switch node "📧 Which Provider?" that checks config.EMAIL_PROVIDER (from Load Config). Values: "convertkit" or "mailerlite" (case-insensitive). Only one branch runs per execution.
- ConvertKit branch: Single "Add to ConvertKit" node. Use ConvertKit API to add subscriber to a sequence (or form that adds to sequence). Use sequence_id or form_id from config (e.g. config.CONVERTKIT_SEQUENCE_ID or CONVERTKIT_FORM_ID), not hardcoded. Replace YOUR_CONVERTKIT_API_SECRET with n8n credential reference (e.g. $credentials.convertKitApi.apiSecret or HTTP header from credentials). Request body: email, first_name, tags; and sequence_id or form subscription so ESP sends first email. (ConvertKit v3 subscribe endpoint or v4 add to sequence — use one that triggers sequence.)
- MailerLite branch: Single "Add to MailerLite" node. Add subscriber with group IDs from config (e.g. config.MAILERLITE_GROUP_ID). Replace YOUR_MAILERLITE_KEY and YOUR_GROUP_ID with n8n credentials and config. Ensure automation in MailerLite is set to "subscriber_joins_group" so first email is sent by ESP.
- Remove or bypass: "🤖 AI: Write Welcome Email 1", "🤖 AI: Write Welcome Email 2", "🤖 AI: Write Welcome Email 3", "🤖 AI: Write Welcome Emails 4+5", "📦 Build Email Sequence", "📊 Log Sequence to Sheets" from the main webhook flow (so first email is not workflow-generated; ESP sends it). Optional: keep "📊 Log Subscriber to Sheets" if desired, using config for documentId/sheetName (no YOUR_GOOGLE_SHEET_ID).
- Success path: After Add to ConvertKit or Add to MailerLite → "✅ Send Success Response" (Respond to Webhook 200 with e.g. { success: true, message: "Welcome! Check your inbox." }). No parallel dual-write: only one ESP node runs per execution.
- Document in node notes: simplified 3-email sequence (e.g. 0, 2, 5 days) is configured in the ESP; workflow only adds subscriber to sequence/group. No YOUR_* in JSON; all secrets and IDs from n8n credentials and config.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('email/v3.0 — Email Newsletter Automation.json')); const body=JSON.stringify(j); if(body.includes('YOUR_CONVERTKIT')||body.includes('YOUR_MAILERLITE')||body.includes('YOUR_GROUP_ID')||body.includes('YOUR_GOOGLE_SHEET_ID')) throw new Error('YOUR_* placeholder still present'); const hasProviderBranch=j.nodes.some(n=>n.name&&(n.name.includes('Provider')||n.name.includes('EMAIL_PROVIDER')||(n.name.includes('ConvertKit')&&n.name.includes('MailerLite')))); if(!j.nodes.some(n=>n.name&&(n.name.includes('ConvertKit')||n.name.includes('MailerLite')))) throw new Error('Missing ESP add node'); console.log('OK');"</automated>
  </verify>
  <done>Single provider branch from EMAIL_PROVIDER; Add to ConvertKit or MailerLite only; no workflow-generated welcome emails; ESP sends first email; no YOUR_* placeholders; Respond 200 after add.</done>
</task>

</tasks>

<verification>
- With EMAIL_NEWSLETTER_ENABLED false: POST to webhook → response 200; no subscriber in ConvertKit/MailerLite.
- With EMAIL_PROVIDER=convertkit and enabled: POST with valid email → subscriber in ConvertKit sequence; first sequence email sent by ESP within ~5 min.
- With EMAIL_PROVIDER=mailerlite and enabled: POST with valid email → subscriber in MailerLite group; automation sends first email.
</verification>

<success_criteria>
- Webhook responds 200 when disabled without adding to ESP. When enabled, one provider branch runs, subscriber added to sequence/group, ESP sends first welcome and subsequent emails on schedule.
</success_criteria>

<output>
After completion, create .planning/phases/04-content-satellites/04-02-SUMMARY.md
</output>
