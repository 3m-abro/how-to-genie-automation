---
phase: 04-content-satellites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - growth/HowTo-Genie v4.0 — Video Production Engine.json
  - social/14_Video_Production.json
autonomous: true
requirements:
  - GROW-05
user_setup: []
must_haves:
  truths:
    - "Video workflow runs at 10:30 AM when VIDEO_PRODUCTION_ENABLED is true"
    - "When disabled, workflow exits without reading Sheets or calling Blotato"
    - "Today's post (date = today, status ≠ publish_failed) is used; when none, exit clean (no Video Log append)"
    - "One post produces multiple scripts (e.g. TikTok, YT Short); each submitted to Blotato via 14_Video_Production"
    - "Video Log has one row per video with date, post_url, post_title, platform, script_type, job_id_or_video_id, status, created_at"
  artifacts:
    - path: growth/HowTo-Genie v4.0 — Video Production Engine.json
      provides: 10:30 scheduler, config gate, today's post filter, LLM scripts, loop to 14_Video_Production
    - path: social/14_Video_Production.json
      provides: Blotato generate/publish and Video Log row with required columns
  key_links:
    - from: Video Production Engine
      to: core/01_Config_Loader.json
      via: Execute Workflow at start
      pattern: Execute Workflow.*Config Loader
    - from: Video Production Engine
      to: social/14_Video_Production.json
      via: Execute Workflow per script with script/title/platform/blog_url/keyword
      pattern: Execute Workflow.*14_Video_Production
    - from: 14_Video_Production
      to: Video Log sheet
      via: Build Video Log Row → Log (VIDEO_LOG_TAB from config)
      pattern: VIDEO_LOG_TAB|Build Video Log Row
---

<objective>
Activate the video production satellite: a 10:30 AM scheduled workflow that reads today's published post from Content Log, generates multiple platform-specific scripts via LLM, submits each to Blotato via the existing 14_Video_Production sub-workflow, and ensures one row per video in Video Log with confirmation ID (job_id_or_video_id).

Purpose: Every published post automatically spawns TikTok/Shorts (and optionally IG Reel) video scripts and videos without manual work; Blotato used per user decision (no Pictory/InVideo).
Output: New workflow JSON (Video Production Engine), updated 14_Video_Production with correct Video Log row shape.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-satellites/04-CONTEXT.md
@.planning/phases/04-content-satellites/04-RESEARCH.md

Reference patterns: growth/HowTo-Genie v4.0 — Multi-Language Expansion Engine.json (Load Config, normalize flag, Filter today's post, Content Log read); social/14_Video_Production.json (Entry, Load Config, Validate+Prepare, Blotato Generate/Publish, Extract Video URL, Notify, Log). Config Loader returns GOOGLE_SHEET_ID, CONTENT_LOG_TAB, VIDEO_PRODUCTION_ENABLED, VIDEO_LOG_TAB, VIDEO_VOICE, ATP_LANGUAGE. 14_Video_Production expects input: script (required), title (required), platform (default tiktok), blog_url, keyword, duration (optional).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Video Production Engine workflow</name>
  <files>growth/HowTo-Genie v4.0 — Video Production Engine.json</files>
  <action>
Create new n8n workflow JSON:
- Name: "HowTo-Genie v4.0 — Video Production Engine".
- Schedule Trigger: cron 30 10 * * * (10:30 AM daily).
- First node: Execute Workflow → core/01_Config_Loader (same pattern as Multi-Language); no workflowId placeholder — use id from 01_Config_Loader.json or document "set workflowId after import".
- Code: Normalize VIDEO_PRODUCTION_ENABLED (true / "true" → true). Same pattern as 04-RESEARCH.md "Normalize enable flag".
- IF: VIDEO_PRODUCTION_ENABLED === true → false branch: workflow ends (no nodes after; no read Sheets, no Blotato). True branch continues.
- Google Sheets: Read rows from documentId = config.GOOGLE_SHEET_ID, sheetName = config.CONTENT_LOG_TAB || 'Content Log'. All rows (or limit ~100).
- Code: Filter today's post — reuse logic from 04-RESEARCH.md "Filter today's post": CONTENT_DAY_TIMEZONE or TIMEZONE or 'UTC'; today = toLocaleDateString('en-CA', { timeZone }); filter rows where date (first 10 chars) === today and status (lowercased) !== 'publish_failed'; if no rows return [{ json: { noPostToday: true } }]; else return latest row with slug (from wp_url), noPostToday: false. Detect column keys case-insensitively (dateKey, statusKey, urlKey).
- IF: noPostToday === true → exit clean (no append to Video Log, no "Skipped" row required per CONTEXT). True branch continues.
- HTTP Request: GET WordPress post body. URL from config (WORDPRESS_URL or equivalent) + slug; or use wp_url from row to derive full HTML. Goal: post body text for LLM. Use credentials for WordPress if needed; no YOUR_* in URL.
- LLM (Ollama): Generate video scripts. Prompt must start with "Return only valid JSON. No text. No markdown." Schema: { success: true, data: { scripts: [ { platform: string, script: string, title: string } ] }, error: null }. Request at least TikTok and YT Short (and optionally ig_reel). Input: post title + post body (or summary). Use qwen2.5:7b or llama3.2 per project; temperature ~0.7.
- Code: Parse & Validate — extract JSON from LLM response (regex for ```json or first {...}); validate scripts array; fallback to single default script on parse error; return envelope { success, data: { scripts }, error }.
- Split/Loop: For each item in data.scripts (or use Loop Over Items / multiple outputs).
- Execute Workflow: Call social/14_Video_Production (by ID/name after import). Pass for each script: script, title, platform (from item), blog_url (from today's row wp_url), keyword (from row or post title). Input must match Validate+Prepare expectations.
- No YOUR_* placeholders in JSON; credentials via n8n (Google Sheets, HTTP, Blotato in sub-workflow).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='growth/HowTo-Genie v4.0 — Video Production Engine.json'; const j=JSON.parse(fs.readFileSync(p)); const names=j.nodes.map(n=>n.name); if(!names.some(n=>/Load Config|Config Loader/i.test(n))) throw new Error('Missing Config Loader'); if(!names.some(n=>/VIDEO_PRODUCTION_ENABLED|Normalize|enable/i.test(n))) throw new Error('Missing enable gate'); if(!names.some(n=>/today|Filter|Content Log/i.test(n))) throw new Error('Missing today filter'); if(!names.some(n=>/script|LLM|Ollama/i.test(n))) throw new Error('Missing LLM scripts'); if(!names.some(n=>/Execute Workflow|14_Video|Video Production/i.test(n))) throw new Error('Missing Execute 14_Video_Production'); console.log('OK');"</automated>
  </verify>
  <done>Video Production Engine workflow exists; Schedule 10:30; Config Loader → VIDEO_PRODUCTION_ENABLED gate → today's post filter → fetch post → LLM scripts → Parse & Validate → loop → Execute 14_Video_Production per script. When disabled or no post, exit without writing to Video Log.</done>
</task>

<task type="auto">
  <name>Task 2: Fix 14_Video_Production so Video Log has one row per video with required columns</name>
  <files>social/14_Video_Production.json</files>
  <action>
In social/14_Video_Production.json:
- Add a new Code node "Build Video Log Row" that builds a single object: { date, post_url, post_title, platform, script_type, job_id_or_video_id, status, created_at }. Source data: from "Validate + Prepare" (title, platform, blog_url → post_title, post_url, platform, script_type); from "Extract Video URL" (video_id → job_id_or_video_id); from Publish response (success/id/post_id → status = 'published' or 'generated'); date = new Date().toISOString().slice(0,10); created_at = new Date().toISOString(). Use $('Validate + Prepare').item.json, $('Extract Video URL').item.json, and $input.first().json (Publish output). Handle missing video_id by using Extract's video_url or Publish response id.
- Wire "Build Video Log Row" so it receives input from "Blotato: Publish Video" (same as "Build Success Notify"). So: Publish has two outgoing connections — one to Build Success Notify (unchanged), one to Build Video Log Row.
- Change "Log Video Production" Google Sheets node to receive input from "Build Video Log Row" instead of from "Notify Video Status". So: disconnect Log from Notify; connect Build Video Log Row → Log Video Production. Log continues to use documentId and sheetName from config (GOOGLE_SHEET_ID, VIDEO_LOG_TAB || 'Video Log'); set columns to explicit mapping from Build Video Log Row fields (date, post_url, post_title, platform, script_type, job_id_or_video_id, status, created_at) so the appended row has exactly these columns. Do not use autoMapInputData with Notify payload.
- Ensure no YOUR_* or hardcoded sheet IDs; documentId/sheetName from $('⚙️ Load Config').item.json.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const j=JSON.parse(fs.readFileSync('social/14_Video_Production.json')); const names=j.nodes.map(n=>n.name); const conns=j.connections||{}; const logFrom=Object.keys(conns).find(n=>conns[n].main&&conns[n].main[0]&&conns[n].main[0].some(c=>c.node&&c.node.includes('Log'))); const logIncoming=Object.entries(conns).filter(([_,v])=>v.main&&v.main[0]&&v.main[0].some(c=>c.node&&c.node.includes('Log Video'))); if(!names.some(n=>/Build Video Log Row/i.test(n))) throw new Error('Missing Build Video Log Row'); const logNode=j.nodes.find(n=>n.name&&n.name.includes('Log Video Production')); if(!logNode) throw new Error('Missing Log node'); console.log('OK');"</automated>
  </verify>
  <done>Video Log receives one row per video with columns date, post_url, post_title, platform, script_type, job_id_or_video_id, status, created_at; Log input is from Build Video Log Row, not from Notify.</done>
</task>

</tasks>

<verification>
- Run Video Production Engine manually in n8n with VIDEO_PRODUCTION_ENABLED true and Content Log having today's row (status ≠ publish_failed): Video Log should get one row per script (e.g. 2 rows for tiktok + yt_short).
- Run with VIDEO_PRODUCTION_ENABLED false: no Content Log read, no Blotato call.
- Run with no today's post or status = publish_failed: exit clean, no Video Log append.
</verification>

<success_criteria>
- Video Production Engine runs at 10:30 AM; config-gated; today's post → multiple scripts → Blotato via 14_Video_Production; Video Log one row per video with confirmation ID (job_id_or_video_id).
</success_criteria>

<output>
After completion, create .planning/phases/04-content-satellites/04-01-SUMMARY.md
</output>
