---
phase: 05-live-dashboards-monitoring
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - laravel/app/Services/TelegramAlertService.php
  - laravel/app/Console/Commands/N8nFailureMonitorCommand.php
  - laravel/app/Console/Kernel.php
  - laravel/routes/console.php
autonomous: true
requirements:
  - DASH-03
user_setup:
  - service: telegram
    why: "Failure alerts (same channel as Phase 1 QC/publish_failed)"
    env_vars:
      - name: TELEGRAM_BOT_TOKEN
        source: "BotFather"
      - name: TELEGRAM_CHAT_ID
        source: "Chat or channel ID for alerts"
must_haves:
  truths:
    - "When any scheduled workflow fails, a Telegram alert arrives within ~10 minutes (workflow name, error, timestamp)"
  artifacts:
    - path: laravel/app/Console/Commands/N8nFailureMonitorCommand.php
      provides: "Poll n8n executions, detect errors, send Telegram"
    - path: laravel/app/Services/TelegramAlertService.php
      provides: "sendMessage to Telegram (or inline Http::post)"
  key_links:
    - from: N8nFailureMonitorCommand
      to: "n8n /api/v1/executions"
      via: "Http with status=error or list then filter"
      pattern: "executions|status.*error"
    - from: N8nFailureMonitorCommand
      to: "Telegram"
      via: "TelegramAlertService or Http::post(api.telegram.org)"
      pattern: "sendMessage|telegram"
---

<objective>
Implement the failure monitor that polls n8n executions, detects failed or missing expected runs, and sends a Telegram alert within ~10 minutes with workflow name, error message, and timestamp (DASH-03). All alerts go to Telegram per CONTEXT; no Discord/Slack in scope.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-live-dashboards-monitoring/05-CONTEXT.md
@.planning/phases/05-live-dashboards-monitoring/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TelegramAlertService and N8nFailureMonitorCommand</name>
  <files>laravel/app/Services/TelegramAlertService.php, laravel/app/Console/Commands/N8nFailureMonitorCommand.php</files>
  <action>
    Create TelegramAlertService that sends one message via Telegram Bot API: Http::post('https://api.telegram.org/bot' . config('services.telegram.bot_token') . '/sendMessage', ['chat_id' => config('services.telegram.chat_id'), 'text' => $message, 'parse_mode' => 'HTML']). No SDK required (RESEARCH). Use for failure alerts only; same channel as Phase 1 QC/publish_failed.
    Create N8nFailureMonitorCommand (signature e.g. n8n:check-failures). In handle(): call n8n GET /api/v1/executions with status=error and limit=50 (use Http::withHeaders(['X-N8N-API-KEY' => config('services.n8n.api_key')]), config('services.n8n.base_url')). For each execution in the response, send one Telegram alert containing: workflow name (from workflowData.name or workflowId), error message (message or stoppedAt reason), timestamp (startedAt). Optional: maintain a small "already alerted" cache (e.g. execution id) for the last 24h to avoid duplicate alerts for the same run; planner's choice. Do not cache the monitor result (RESEARCH pitfall: cache only for dashboard APIs). If executions list does not include status, fetch by id or filter client-side. Log failures to send Telegram.
  </action>
  <verify>
    <automated>cd laravel && php artisan test --filter=N8nFailureMonitor</automated>
  </verify>
  <done>Command fetches error executions from n8n and sends Telegram alert per failure; test mocks n8n and Telegram.</done>
</task>

<task type="auto">
  <name>Task 2: Schedule monitor every 5–10 minutes</name>
  <files>laravel/app/Console/Kernel.php</files>
  <action>
    Register N8nFailureMonitorCommand in the Laravel scheduler: run every 5 or 10 minutes (e.g. $schedule->command('n8n:check-failures')->everyFiveMinutes();). Use app/Console/Kernel.php schedule() method. Document that cron must run Laravel scheduler (php artisan schedule:run) if not already configured. No change to existing workflow logic in n8n; monitor is Laravel-only.
  </action>
  <verify>
    <automated>grep -E "n8n:check-failures|everyFiveMinutes|schedule" laravel/app/Console/Kernel.php</automated>
  </verify>
  <done>Scheduler runs failure monitor every 5–10 min; owner receives Telegram within ~10 min of workflow failure.</done>
</task>

</tasks>

<verification>
- php artisan n8n:check-failures runs without error (with mocked or real n8n/Telegram).
- Scheduler entry present for n8n:check-failures.
- php artisan test --filter=N8nFailureMonitor passes.
</verification>

<success_criteria>
- DASH-03 satisfied: System health monitor sends Telegram alert when any scheduled workflow fails (workflow name, error, timestamp).
</success_criteria>

<output>
After completion, create .planning/phases/05-live-dashboards-monitoring/05-03-SUMMARY.md
</output>
