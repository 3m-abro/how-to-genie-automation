---
phase: 05-live-dashboards-monitoring
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - laravel/app/Services/N8nApiService.php
  - laravel/app/Http/Controllers/MissionControlController.php
  - laravel/routes/api.php
  - ui/adhd-mission-control.tsx
autonomous: true
requirements:
  - DASH-02
must_haves:
  truths:
    - "ADHD Mission Control dashboard shows real n8n workflow run statuses (last run, next run, status)"
  artifacts:
    - path: laravel/app/Services/N8nApiService.php
      provides: "Workflows list and executions (last run outcome) from n8n API"
    - path: laravel/app/Http/Controllers/MissionControlController.php
      provides: "Mission control data with modules (last run, next run, active)"
  key_links:
    - from: ui/adhd-mission-control.tsx
      to: "/api/n8n/status or /api/dashboard/mission-control"
      via: "fetch"
      pattern: "fetch.*n8n|api.*mission-control"
    - from: MissionControlController or N8nApiService
      to: "n8n /api/v1/workflows and /api/v1/executions"
      via: "Http::withHeaders X-N8N-API-KEY"
      pattern: "api/v1/workflows|api/v1/executions"
---

<objective>
Implement n8n-backed mission control API and wire the ADHD Mission Control dashboard so the owner sees real workflow statuses: last execution result (success/failure + time), next run, and active state per module (DASH-02).
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-live-dashboards-monitoring/05-CONTEXT.md
@.planning/phases/05-live-dashboards-monitoring/05-RESEARCH.md
@ui/laravel-admin-panel.php
@ui/adhd-mission-control.tsx
</context>

<interfaces>
From CONTEXT and RESEARCH: Mission control data shape must include per-module: last_run (outcome + time), next_run, status (running/stopped/error). Source: GET /api/v1/workflows (active, updatedAt) and GET /api/v1/executions (per workflow or filtered by status) for last run outcome. Use X-N8N-API-KEY header; base URL from config. 5-min cache acceptable.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: N8nApiService and mission-control API endpoint</name>
  <files>laravel/app/Services/N8nApiService.php, laravel/app/Http/Controllers/MissionControlController.php, laravel/routes/api.php or web.php</files>
  <action>
    Create N8nApiService that calls n8n with Http::withHeaders(['X-N8N-API-KEY' => config('services.n8n.api_key')]), timeout(5). Methods: getWorkflows() (GET /api/v1/workflows), getExecutions($workflowId = null, $status = null, $limit = 50). Use config('services.n8n.base_url'). Do not hardcode URL or key (RESEARCH pitfall).
    Extend MissionControlController (or add DashboardController) so the mission-control payload comes from n8n: list workflows, for each workflow optionally fetch last execution (GET /api/v1/executions?workflowId={id}&limit=1) to get last run outcome (success/error) and startedAt. Map to "modules" array: name, status (running/stopped/error from workflow.active and last execution status), last_run (time + outcome), next_run (Scheduled or heuristic from cron if available). Replace existing getSystemStatus()/checkN8nWorkflow() logic that uses only workflow.updatedAt with executions-based last run. Return structure compatible with ADHD dashboard: system_status.overall, system_status.modules, today_progress, weekly_wins, priorities, etc. Use Cache::remember(300) for this payload. Expose GET /api/n8n/status or GET /api/dashboard/mission-control returning this JSON.
    Handle n8n list response shape: if executions list omits status (RESEARCH pitfall), fetch last run per workflow or use GET /executions/{id} for status. Implement or update MissionControlApiTest to assert 200 and response has system_status or modules with at least one workflow.
  </action>
  <verify>
    <automated>cd laravel && php artisan test --filter=MissionControl</automated>
  </verify>
  <done>Mission control API returns workflow list with last run outcome and status from n8n; test green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ADHD Mission Control UI to API</name>
  <files>ui/adhd-mission-control.tsx</files>
  <action>
    Replace hardcoded systemStatus, weeklyWins, priorities in ui/adhd-mission-control.tsx with data fetched from the Laravel API (GET /api/n8n/status or /api/dashboard/mission-control). Use useEffect + fetch with configurable base URL. Map API response to existing component props (systemStatus.modules, weeklyWins, priorities). Handle loading and error state. Preserve pomodoro and existing layout; only data source changes.
  </action>
  <verify>
    <automated>grep -E "fetch|useEffect|api/n8n|mission-control" ui/adhd-mission-control.tsx | head -5</automated>
  </verify>
  <done>ADHD dashboard shows real system status from n8n; no hardcoded systemStatus/weeklyWins/priorities.</done>
</task>

</tasks>

<verification>
- GET /api/n8n/status or /api/dashboard/mission-control returns 200 with modules and last run data from n8n.
- ADHD dashboard UI shows live data (manual: open dashboard, confirm modules reflect n8n).
- php artisan test --filter=MissionControl passes.
</verification>

<success_criteria>
- DASH-02 satisfied: ADHD Mission Control dashboard shows real system status from n8n API (workflows + executions).
</success_criteria>

<output>
After completion, create .planning/phases/05-live-dashboards-monitoring/05-02-SUMMARY.md
</output>
