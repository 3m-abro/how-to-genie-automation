---
phase: 02-distribution-growth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - growth/HowTo-Genie v4.0 — WhatsApp & Telegram Distribution Bot.json
autonomous: true
requirements:
  - GROW-02
user_setup: []
must_haves:
  truths:
    - "Messaging digest workflow runs at 10 AM when MESSAGING_DIGEST_ENABLED is true"
    - "When disabled, workflow exits without writing to Messaging Distribution Log"
    - "When no valid post today or zero active subscribers, workflow appends exactly one Skipped row to Messaging Distribution Log then exits"
    - "When post and subscribers exist, digest (title + link + optional hook) is sent via Telegram (and WhatsApp if enabled) and one Sent row is logged"
  artifacts:
    - path: growth/HowTo-Genie v4.0 — WhatsApp & Telegram Distribution Bot.json
      provides: "Config-gated daily digest to Telegram/WhatsApp subscribers"
      contains: "Execute Workflow (Config Loader), IF MESSAGING_DIGEST_ENABLED, Get today's post filter, skip paths, subscriber load, send, log"
  key_links:
    - from: "Schedule Trigger"
      to: "Execute Workflow (Config Loader)"
      via: "first node after trigger"
      pattern: "executeWorkflow.*Config Loader"
    - from: "Get today's post Code node"
      to: "Content Log"
      via: "filter date === today (config TZ), status !== publish_failed"
      pattern: "noPostToday|valid.*row"
    - from: "Load Messaging Subscribers"
      to: "config"
      via: "MESSAGING_SUBSCRIBERS_TAB, GOOGLE_SHEET_ID from $('⚙️ Load Config').item.json"
    - from: "Log Distribution / Skipped row"
      to: "Messaging Distribution Log tab"
      via: "config sheet ID and tab name"
---

<objective>
Activate the WhatsApp & Telegram Distribution Bot so it runs at 10 AM only when enabled in config, uses today's published post from Content Log, loads subscribers from config-driven tab, and sends daily digest (title + link + optional 1-line hook); when no post or zero subscribers, appends exactly one Skipped row to Messaging Distribution Log then exits.

Purpose: GROW-02 — today's post reaches Telegram (and WhatsApp when enabled) subscribers daily; enable/disable via htg_config only.
Output: Modified workflow JSON with Config Loader at start, enable gate, today+status filter, skip logging, subscriber columns (Platform, Chat ID or Phone, Status), and config-driven sheet/tabs.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-distribution-growth/02-CONTEXT.md
@.planning/phases/02-distribution-growth/02-RESEARCH.md
@.planning/phases/02-distribution-growth/02-VALIDATION.md

Reference: core/08_Orchestrator_v3.json — "⚙️ Load Config" (executeWorkflow) first; downstream use $('⚙️ Load Config').item.json. Subscriber sheet: same Google Sheet as Content Log; tab from MESSAGING_SUBSCRIBERS_TAB. Columns (min): Platform (WhatsApp/Telegram), Chat ID or Phone, Language (optional, default en), Status (active/inactive). Telegram uses chat_id; WhatsApp uses E.164 phone. When no post: append one row Status=Skipped, reason=no_post_today. When zero active subscribers: append one row Status=Skipped, Recipients=0.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config Loader, enable gate, Get today's post, and skip paths</name>
  <files>growth/HowTo-Genie v4.0 — WhatsApp & Telegram Distribution Bot.json</files>
  <action>
    Insert Execute Workflow as the first node after "📱 Daily Distribution (10AM All Timezones)". Configure it to run the Config Loader sub-workflow (core/01_Config_Loader.json); name the node "⚙️ Load Config". Use default wait for sub-workflow. Add a Code node that normalizes MESSAGING_DIGEST_ENABLED (true or string "true" → true). Add an IF node: MESSAGING_DIGEST_ENABLED === true; false branch stops (no log). Replace "📄 Get Today's Post" with Google Sheets read from config GOOGLE_SHEET_ID/SPREADSHEET_ID and CONTENT_LOG_TAB. Add a Code node "Filter today's post" (same pattern as 02-01: timezone from config, date === today, status !== publish_failed). When no valid row: append exactly one row to Messaging Distribution Log (sheet ID and tab from config; add key MESSAGING_DISTRIBUTION_LOG_TAB or equivalent with fallback "Messaging Distribution Log") with Status = "Skipped", reason = "no_post_today", then stop. When valid row exists, continue to "👥 Load Messaging Subscribers". Replace Load Messaging Subscribers to use config GOOGLE_SHEET_ID and MESSAGING_SUBSCRIBERS_TAB (fallback "Messaging Subscribers"). Add logic after loading subscribers: filter rows where Status === 'active' (case-insensitive); if zero active, append exactly one row to Messaging Distribution Log with Status = "Skipped", Recipients = 0, then stop. Do not log when workflow is disabled (IF false branch).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('growth/HowTo-Genie v4.0 — WhatsApp & Telegram Distribution Bot.json'))"</automated>
  </verify>
  <done>Workflow has ⚙️ Load Config → Code (normalize) → IF enabled → Sheets (Content Log) → Filter today's post; no-post path appends one Skipped row (no_post_today) and stops; Load Subscribers from config tab; zero-subscriber path appends one Skipped row (Recipients=0) and stops.</done>
</task>

<task type="auto">
  <name>Task 2: Subscriber columns, Telegram/WhatsApp routing, and distribution log</name>
  <files>growth/HowTo-Genie v4.0 — WhatsApp & Telegram Distribution Bot.json</files>
  <action>
    Ensure subscriber sheet columns are used as: Platform (WhatsApp/Telegram), "Chat ID or Phone" (one column — Telegram rows use chat_id, WhatsApp rows use E.164 phone), Status (active/inactive), Language optional. Update "👤 Personalize for Each Subscriber" (or equivalent) to read Platform and "Chat ID or Phone" (or Identifier) from each row; pass chat_id to Telegram send node and phone/E.164 to WhatsApp send node. Route by Platform: Telegram branch uses chat_id from the column; WhatsApp branch uses phone from the column; only send Telegram until WHATSAPP_DIGEST_ENABLED is true (per CONTEXT — optional config key; if missing or false, route WhatsApp subscribers to a no-op or skip). Ensure "📊 Log Distribution" appends to Messaging Distribution Log using config sheet ID and MESSAGING_DISTRIBUTION_LOG_TAB; log row includes Status (Sent/Skipped), Recipients count, reason when Skipped. Parse & Validate after "💬 AI: Adapt for Messaging" per n8n-json-contracts: extract JSON, fallback to post title + URL if parse fails.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('growth/HowTo-Genie v4.0 — WhatsApp & Telegram Distribution Bot.json'))"</automated>
  </verify>
  <done>Subscriber columns Platform, Chat ID or Phone, Status drive routing; Telegram gets chat_id, WhatsApp gets E.164; distribution log uses config; LLM output parsed with fallback.</done>
</task>

</tasks>

<verification>
- Run with MESSAGING_DIGEST_ENABLED=false or missing: no Messaging Distribution Log row (manual).
- Run with no valid post today: exactly one row Status=Skipped, reason=no_post_today (manual).
- Run with zero active subscribers: exactly one row Status=Skipped, Recipients=0 (manual).
- Run with valid post and ≥1 active subscriber: messages sent, one Sent row in log (manual).
</verification>

<success_criteria>
- Messaging workflow runs at 10 AM when enabled; when disabled, no log row.
- No post or zero subscribers → one Skipped row then exit.
- Post + subscribers → digest (title + link + optional hook) sent via Telegram (and WhatsApp if enabled), one Sent row in Messaging Distribution Log.
- All sheet IDs and tabs from Config Loader; toggling MESSAGING_DIGEST_ENABLED turns workflow on/off without re-importing JSON.
</success_criteria>

<output>
After completion, create .planning/phases/02-distribution-growth/02-02-SUMMARY.md
</output>
