---
phase: 06-affiliate-seo-feedback
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - content/v4.0 — SEO Interlinking Intelligence Engine.json
autonomous: true
requirements:
  - SEO-02
user_setup: []

must_haves:
  truths:
    - "SEO Interlinking workflow runs every Sunday at 3 AM (cron 0 3 * * 0)"
    - "Workflow uses Config Loader first; all sheet IDs and tab names come from config (no YOUR_*)"
    - "Internal link recommendations are written to the Internal Linking Log tab (config-driven)"
  artifacts:
    - path: content/v4.0 — SEO Interlinking Intelligence Engine.json
      provides: "SEO Interlinking workflow with config-driven sheet/tab and optional WP update"
      contains: "Execute Workflow Config Loader, documentId/sheetName from config, INTERNAL_LINKING_LOG_TAB"
  key_links:
    - from: "content/v4.0 — SEO Interlinking Intelligence Engine.json"
      to: "Config Loader"
      via: "Execute Workflow node after Schedule Trigger"
      pattern: "Load Config|executeWorkflow"
    - from: "content/v4.0 — SEO Interlinking Intelligence Engine.json"
      to: "Google Sheets"
      via: "documentId and sheetName from $('⚙️ Load Config').item.json"
      pattern: "GOOGLE_SHEET_ID|INTERNAL_LINKING_LOG_TAB|CONTENT_LOG_TAB"
---

<objective>
Wire the existing SEO Interlinking Intelligence Engine to Config Loader and config-driven sheet/tab names. Remove all YOUR_GOOGLE_SHEET_ID and hardcoded tab names so the workflow runs with the same config pattern as other Phase 6 workflows. Satisfies SEO-02: engine runs Sunday 3 AM and writes internal link recommendations to a Sheets tab (and optionally updates WordPress).
Purpose: No placeholder IDs in production; single config source for sheet and tabs.
Output: Updated content/v4.0 — SEO Interlinking Intelligence Engine.json.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-affiliate-seo-feedback/06-CONTEXT.md
@.planning/phases/06-affiliate-seo-feedback/06-RESEARCH.md
@.planning/phases/06-affiliate-seo-feedback/06-CONFIG-KEYS.md
content/v4.0 — SEO Interlinking Intelligence Engine.json
core/01_Config_Loader.json
</context>

<interfaces>
06-CONFIG-KEYS: GOOGLE_SHEET_ID (or SPREADSHEET_ID), CONTENT_LOG_TAB (for Load All Published Posts), INTERNAL_LINKING_LOG_TAB, optional SEO_INTERLINKING_ENABLED, SEO_STRATEGY_TAB or keep "SEO Strategy" as literal. Current workflow: Schedule → Load All Published Posts (documentId YOUR_GOOGLE_SHEET_ID) → Build Content Index → ... → Log Linking Updates (documentId, sheetName "Internal Linking Log"), Log SEO Strategy Analysis (documentId, "SEO Strategy"). WordPress update node may use WORDPRESS_URL from config if present.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Insert Config Loader and replace hardcoded sheet/tab in read nodes</name>
  <files>content/v4.0 — SEO Interlinking Intelligence Engine.json</files>
  <action>
Edit content/v4.0 — SEO Interlinking Intelligence Engine.json:

1. **Insert Execute Workflow (Config Loader) after Schedule Trigger:** Add a node "⚙️ Load Config" (type n8n-nodes-base.executeWorkflow) that runs core/01_Config_Loader (workflowId reference: REPLACE_WITH_CONFIG_LOADER_ID or id). Position between "🔗 Weekly Interlinking Scan (Sunday 3AM)" and "📊 Load All Published Posts". Update connections so: Schedule → Load Config → Load All Published Posts (and rest of flow unchanged).

2. **Replace "📊 Load All Published Posts" parameters:** Change documentId from "YOUR_GOOGLE_SHEET_ID" to expression: "={{ $('⚙️ Load Config').item.json.GOOGLE_SHEET_ID || $('⚙️ Load Config').item.json.SPREADSHEET_ID }}". Add sheetName (or range) from config: "={{ $('⚙️ Load Config').item.json.CONTENT_LOG_TAB || 'Content Log' }}". Use the correct parameter names for the Google Sheets node (operation readRange; documentId and sheetName/value as per n8n version).

3. **Optional IF — SEO_INTERLINKING_ENABLED:** After Load Config, add IF node that checks config.SEO_INTERLINKING_ENABLED === 'true'; true branch continues to Load All Published Posts, false branch ends. Per CONTEXT planner may choose; if omitted, workflow always runs when triggered.

Ensure no literal "YOUR_GOOGLE_SHEET_ID" or "your-blog.com" remains in the file after this task.
  </action>
  <verify>
    <automated>grep -q "Load Config\|GOOGLE_SHEET_ID\|SPREADSHEET_ID" "content/v4.0 — SEO Interlinking Intelligence Engine.json" && ! grep -q "YOUR_GOOGLE_SHEET_ID" "content/v4.0 — SEO Interlinking Intelligence Engine.json"</automated>
  </verify>
  <done>Config Loader is first after Schedule; Load All Published Posts uses config for documentId and CONTENT_LOG_TAB.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded sheet/tab in append (log) nodes and WP URL</name>
  <files>content/v4.0 — SEO Interlinking Intelligence Engine.json</files>
  <action>
In the same workflow JSON:

1. **"📊 Log Linking Updates" (Internal Linking Log):** Replace documentId "YOUR_GOOGLE_SHEET_ID" with "={{ $('⚙️ Load Config').item.json.GOOGLE_SHEET_ID || $('⚙️ Load Config').item.json.SPREADSHEET_ID }}". Replace sheetName "Internal Linking Log" with "={{ $('⚙️ Load Config').item.json.INTERNAL_LINKING_LOG_TAB || 'Internal Linking Log' }}". Note: This node may receive input from a different branch; ensure the node can still resolve Load Config (e.g. all branches that reach this node have run through Load Config earlier, or use a static reference to the config node by name).

2. **"📈 Log SEO Strategy Analysis" (SEO Strategy):** Replace documentId with same config expression. Replace sheetName "SEO Strategy" with config key if documented in 06-CONFIG-KEYS (e.g. SEO_STRATEGY_TAB || 'SEO Strategy'), or leave as literal 'SEO Strategy' if not in config doc.

3. **WordPress update node ("🔄 Update WordPress Post"):** If the URL is hardcoded, replace with "={{ $('⚙️ Load Config').item.json.WORDPRESS_URL || 'https://example.com' }}". Ensure no your-blog.com literal remains.

4. **Schedule:** Confirm cron remains "0 3 * * 0" (Sunday 3 AM). No change if already set.

Re-save the JSON with valid connections so the flow is: Schedule → Load Config [→ IF enabled] → Load All Published Posts → ... → Log Linking Updates / Log SEO Strategy / WP Update. Fix any connection edges so Load Config output is available to nodes that need it (n8n passes data along connections; if Log nodes are in a different branch, they may need to reference the config node by name which works in n8n when config was run in the same execution).
  </action>
  <verify>
    <automated>! grep -q "YOUR_GOOGLE_SHEET_ID\|your-blog\.com" "content/v4.0 — SEO Interlinking Intelligence Engine.json" && grep -q "INTERNAL_LINKING_LOG_TAB\|Internal Linking Log" "content/v4.0 — SEO Interlinking Intelligence Engine.json"</automated>
  </verify>
  <done>All Google Sheets and WP URL references use config; SEO-02 satisfied.</done>
</task>

</tasks>

<verification>
- Manual: Run SEO Interlinking workflow (trigger or execute); confirm it reads from config and writes to Internal Linking Log tab.
</verification>

<success_criteria>
- SEO Interlinking engine runs Sunday 3 AM; uses Config Loader; all documentId and sheetName (and WORDPRESS_URL if used) come from config; no YOUR_* placeholders in file.
</success_criteria>

<output>
After completion, create .planning/phases/06-affiliate-seo-feedback/06-04-SUMMARY.md
</output>
