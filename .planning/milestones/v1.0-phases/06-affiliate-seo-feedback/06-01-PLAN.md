---
phase: 06-affiliate-seo-feedback
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-affiliate-seo-feedback/06-CONFIG-KEYS.md
autonomous: true
requirements:
  - AFF-01
user_setup: []

must_haves:
  truths:
    - "Phase 6 config keys are documented and can be added to htg_config data table"
    - "Affiliate registry tab name and row shape are defined so Manager can write to it"
    - "At least one active product per niche (productivity, finance, home, health, tech) is achievable via Manager run or documented seed"
  artifacts:
    - path: .planning/phases/06-affiliate-seo-feedback/06-CONFIG-KEYS.md
      provides: "Phase 6 config keys, registry row shape, and tab names for htg_config"
      min_lines: 40
  key_links:
    - from: "06-CONFIG-KEYS.md"
      to: "htg_config data table"
      via: "documentation for user to add Key/Value rows"
      pattern: "AFFILIATE_REGISTRY_TAB|REFRESH_CANDIDATES_TAB|INTERNAL_LINKING_LOG_TAB"
---

<objective>
Define Phase 6 configuration keys and the affiliate product registry tab shape so the Affiliate Link Manager (Plan 02) and other workflows can read/write using config only. No YOUR_* or hardcoded sheet IDs in workflows; all tab names and enable flags come from config.
Purpose: Single source of truth for registry columns and config keys; AFF-01 requires registry "populated with current products across all 5 niches" — this plan enables that by defining the contract.
Output: 06-CONFIG-KEYS.md with keys, registry row shape, and optional bootstrap note.
</objective>

<execution_context>
@/home/maqsood/.claude/get-shit-done/workflows/execute-plan.md
@/home/maqsood/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-affiliate-seo-feedback/06-CONTEXT.md
@.planning/phases/06-affiliate-seo-feedback/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 06-CONFIG-KEYS.md with Phase 6 keys and registry shape</name>
  <files>.planning/phases/06-affiliate-seo-feedback/06-CONFIG-KEYS.md</files>
  <action>
Create .planning/phases/06-affiliate-seo-feedback/06-CONFIG-KEYS.md.

Include:
1. **Config keys to add to htg_config (Key/Value):**
   - AFFILIATE_REGISTRY_TAB — sheet tab for product registry (e.g. "Affiliate Registry")
   - AFFILIATE_MANAGER_ENABLED — "true" or "false"; gate for Affiliate Link Manager
   - REFRESH_CANDIDATES_TAB — sheet tab for GA4 refresh-candidates list (e.g. "Refresh Candidates")
   - REFRESH_VIEWS_MIN — optional; minimum 7d views for a post to be a refresh candidate (default 100; can reuse VIRAL_VIEWS_7D_MIN from Viral Amplifier)
   - SEO_INTERLINKING_ENABLED — "true" or "false"; gate for SEO Interlinking workflow
   - INTERNAL_LINKING_LOG_TAB — sheet tab for interlink recommendations log (e.g. "Internal Linking Log")
   - CONTENT_LOG_TAB — already used elsewhere; document that SEO Interlinking reads from it for "Load All Published Posts"
   - NICHES — optional; comma-separated list (e.g. "productivity,finance,home,health,tech") for registry niche column; default five fixed

2. **Affiliate registry row shape (columns):**
   - product_name, platform, commission, url, niche, score, date_found, status
   - niche must be one of: productivity, finance, home, health, tech (or from NICHES config)
   - status: "active" | "deprecated" (or similar)

3. **Required Google Sheets tabs:** List which tabs must exist in the sheet identified by GOOGLE_SHEET_ID / SPREADSHEET_ID: Affiliate Registry, Refresh Candidates, Internal Linking Log (and Content Log if not already present).

4. **Bootstrap / seed:** Note that running the Affiliate Link Manager (Plan 02) will populate the registry from Muncheye/CBEngine RSS; alternatively document one-time manual seed of at least one row per niche if Manager is not yet run.

Do not create or modify workflow JSON in this plan. Config Loader (core/01_Config_Loader.json) already builds a generic key-value object from the data table — no code change needed; this doc is the contract for what keys to add.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-affiliate-seo-feedback/06-CONFIG-KEYS.md && grep -q AFFILIATE_REGISTRY_TAB .planning/phases/06-affiliate-seo-feedback/06-CONFIG-KEYS.md && grep -qE "product_name|niche|registry" .planning/phases/06-affiliate-seo-feedback/06-CONFIG-KEYS.md</automated>
  </verify>
  <done>06-CONFIG-KEYS.md exists with all Phase 6 keys, registry row shape, and required tabs; executor can add these keys to htg_config and create tabs in Sheets.</done>
</task>

</tasks>

<verification>
- 06-CONFIG-KEYS.md is present and contains AFFILIATE_REGISTRY_TAB, REFRESH_CANDIDATES_TAB, INTERNAL_LINKING_LOG_TAB, registry columns (product_name, platform, url, niche, score, date_found, status).
- No workflow JSON modified in this plan.
</verification>

<success_criteria>
- Phase 6 config keys and registry shape are documented; Plan 02 (Affiliate Manager) can reference this doc for tab name and row structure.
</success_criteria>

<output>
After completion, create .planning/phases/06-affiliate-seo-feedback/06-01-SUMMARY.md
</output>
